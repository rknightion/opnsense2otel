[Unit]
Description=Daily OPNsense release-channel schema canary

[Timer]
# 07:20 UTC. Probes the production firewall, not the oli testbed, so it runs
# daily regardless of whether the lab is up.
OnCalendar=*-*-* 07:20:00 UTC
Persistent=true
RandomizedDelaySec=300

[Install]
WantedBy=timers.target
