{
  "hasyncVersion": {
    "note": "#459 audited every endpoint sharing OSPFv3's `Response json.RawMessage` envelope pattern and left this one unwrapped deliberately, not for lack of a body type to point at. hasyncVersionResponse.Response is genuinely polymorphic on the wire: an object ({\"firmware\":{...}}) only when HA is configured and the peer answers; a bare JSON boolean (`false`) on the overwhelming majority of installs (single-node, HA unconfigured \u2014 see HasyncStatusController.php's exception path); JSON null on some error paths. Reflecting through it as an object would misdescribe the common case as a schema conflict rather than widen an unverified path, which is the opposite of what #459 is for. PRUNE TRIGGER: if a future OPNsense release collapses this to one shape, or if per-field polymorphic kinds are ever supported by FieldKind, register an envelopeDescent here like the other FRR/Tor endpoints.",
    "required": [
      {
        "path": "response",
        "opaque": true,
        "metrics": [
          "opnsense_hasync_remote_reachable",
          "opnsense_hasync_remote_version_match",
          "opnsense_hasync_remote_version_info",
          "opnsense_hasync_remote_services",
          "opnsense_hasync_remote_service_running"
        ],
        "exercise": "Two OPNsense boxes on the isolated testbed bridge with hasync pointed at each other (System > High Availability > Settings: synchronize-to-IP = the peer, plus a shared XML-RPC account holding page-all on BOTH nodes - the account is authenticated on the box being CALLED, so it has to exist on both or the endpoint answers {\"response\":\"Authentication failed\"} rather than throwing). This endpoint makes a LIVE XML-RPC call to the peer on every poll, so the peer must be up: stop the second VM, or drop its bridge link, and \"response\" disappears entirely - the controller returns the exception envelope {\"status\":\"error\",\"message\":\"fetch error. remote host down? ...\"} instead. Boot the second VM before treating a regression here as drift.",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460), the first time this endpoint has ever answered from a real HA peer. Both boxes returned a populated response object carrying base/firmware/kernel sub-objects, with firmware holding the peer's version and _my_version holding the local one. BOTH branches of the comparison were observed: the peer was first an identical clone (match) and was then switched to the previous stable release channel while the first box stayed on devel (mismatch), so opnsense_hasync_remote_version_match was seen as 1 and as 0. Stays flagged opaque because hasyncVersionResponse.Response is json.RawMessage -> KindAny and is deliberately NOT unwrapped (see the note below), so the canary can only ever prove the key exists - the inner field evidence is this note plus the hasync_test.go fixtures, not a machine assertion."
      }
    ]
  },
  "quaggaOspfInterface": {
    "note": "#459 audited every endpoint sharing OSPFv3's `Response json.RawMessage` envelope pattern; this one is deliberately left as a bare envelope rather than unwrapped. frr.go's FetchFRROSPFInterfaces decodes TWO candidate shapes for the same endpoint: FRR>=8 wraps per-interface data in {\"interfaces\": {...}} (frrOSPFInterfaceWrapped, confirmed live against this box's FRR 10.3), while older FRR returns the flat per-interface map directly at the top level (frrOSPFInterfaceEntry map) per the plugin's own doc comments \u2014 but that flat shape has never itself been confirmed live. Registering an envelopeDescent requires naming ONE Inner type; picking the wrapped shape would silently assert the flat shape doesn't exist, and picking the flat shape would misdescribe the one shape actually verified. PRUNE TRIGGER: confirm which shape(s) a live box in the support window actually sends (dev-box FRR is 10.3, already wrapped-only), then register an envelopeDescent \u2014 same treatment as quaggaOspfv3Interface below, which has no such ambiguity."
  },
  "dechwPowerStatus": {
    "stateOptional": [
      {
        "path": "pwr1",
        "opaque": true,
        "reason": "Deciso appliance PSU rail. Real hardware only: api/dechw/info/power_status answers {\"status\":\"failed\"} on anything that is not a DEC box with power sensors, so the canary VM can never populate it. The ABSENT branch is already suppressed by the dechwPowerStatus missingOK entry in exemptions.json; this entry classifies the null/empty branch, which lands in Unverified instead of Missing.",
        "pruneTrigger": "delete when a canary box with DEC power sensors exists, or when the dechwPowerStatus missingOK entry is pruned for the same reason"
      },
      {
        "path": "pwr2",
        "opaque": true,
        "reason": "Second Deciso PSU rail; same hardware gate as pwr1.",
        "pruneTrigger": "delete together with the pwr1 entry"
      }
    ],
    "note": "Hardware-only endpoint. Nothing here backs a metric on a VM, so no required class."
  },
  "interfacesOverview": {
    "stateOptional": [
      {
        "path": "rows[].sfp.*",
        "reason": "SFP transceiver diagnostics (DOM). Only a row that is a physical port with a populated SFP cage carries an sfp block; the canary VM is virtio-only. The block-absent branch is already suppressed by the interfacesOverview missingOK entry in exemptions.json; this entry classifies the served-but-empty branch ({} for sfp), which puts the sfp.* subtree in Unverified rather than Missing.",
        "pruneTrigger": "delete when a canary box with a real SFP cage exists (same trigger as the interfacesOverview missingOK sfp entries)"
      }
    ]
  },
  "keaLeases6": {
    "required": [
      {
        "path": "rows[].address",
        "metrics": [
          "opnsense_kea_dhcp6_lease_info",
          "opnsense_kea_dhcp6_leases"
        ],
        "exercise": "Bring up a DHCPv6 client on a Kea-served VLAN of the testbed and let it take an IA_NA lease, so leases6/search returns at least one row.",
        "verified": "LIVE-VERIFIED 2026-07-25: a Debian VM on the isolated testbed bridge took DHCPv6 addresses via Kea, and leases6/search returned total=2 with rows[] populated. The client renews (delegated prefix re-solicited each cycle), so the rows persist while that VM runs."
      },
      {
        "path": "rows[].type",
        "opaque": true,
        "metrics": [
          "opnsense_kea_dhcp6_leases_by_type"
        ],
        "exercise": "Hold BOTH an IA_NA and an IA_PD lease on the testbed, then read the per-row type values out of the runner-local capture (never committed) to confirm the \"IA_NA\"/\"IA_PD\" spelling the label mapping assumes.",
        "verified": "VALUES HUMAN-READ 2026-07-25: both enum values observed live on real rows - \"IA_NA\" and \"IA_PD\" - which is exactly the pair keaLeaseTypeLabel was tolerating blind. Still flagged opaque because the field is a flex type (KindAny), so the canary can only ever prove the key exists; the VALUE check is this note, not a machine assertion."
      }
    ],
    "note": "DHCPv6 lease rows were the last source-derived Kea surface; closed 2026-07-25. ROOT CAUSE of the long-standing gap, recorded so it is not re-investigated: the Proxmox host kernel is booted with ipv6.disable=1, so /proc/sys/net/ipv6 does not exist in ANY namespace on that host and NO LXC on it can ever run a DHCPv6 client. This is a host-kernel property, not container privilege - the earlier #227 conclusion. Only a VM (own kernel) can hold a v6 lease; bridge-level L2 forwarding is unaffected. REGRESSION TRIGGER: these rows expire within the lease lifetime (~4000s) if that VM is stopped. Also note keaLeases4 is currently EMPTY (no DHCPv4 client on the test VLAN) despite a 2026-07-13 capture, so a v4 lease row is now the weaker side."
  },
  "keaPdPools6": {
    "required": [
      {
        "path": "rows[].prefix",
        "metrics": [
          "opnsense_kea_dhcp6_pd_pool_size"
        ],
        "exercise": "Configure one DHCPv6 prefix-delegation pool on the testbed (Services > Kea DHCP > DHCPv6 > PD pools) so api/kea/dhcpv6/searchPdPool returns a non-empty rows array. Configuration only: no client needs to take a delegation.",
        "verified": "LIVE-VERIFIED 2026-07-25: searchPdPool returns total=1 with rows[] populated. The PD pool turned out to be already configured on the testbed - the missing ingredient was a client, not config."
      },
      {
        "path": "rows[].subnet",
        "metrics": [
          "opnsense_kea_dhcp6_pd_pool_size"
        ],
        "exercise": "Same as rows[].prefix. This field carries the subnet UUID that pdPoolSubnetLabel joins against keaSubnets6, so an empty rows array also leaves that join path unexercised.",
        "verified": "LIVE-VERIFIED 2026-07-25: populated, and the pdPoolSubnetLabel join against keaSubnets6 (total=1) is now exercised."
      },
      {
        "path": "rows[].prefix_len",
        "opaque": true,
        "metrics": [
          "opnsense_kea_dhcp6_pd_pool_size"
        ],
        "exercise": "Same as rows[].prefix, then confirm from the runner-local capture that prefix_len arrives as a number or numeric string, since the pool capacity is computed as 2^(delegated_len-prefix_len).",
        "verified": "VALUES HUMAN-READ 2026-07-25: both capacity operands present on the live PD-pool row. Remains opaque (flexInt -> KindAny), so this note is the type evidence, not a canary assertion."
      },
      {
        "path": "rows[].delegated_len",
        "opaque": true,
        "metrics": [
          "opnsense_kea_dhcp6_pd_pool_size"
        ],
        "exercise": "Same as rows[].prefix_len; both operands of the capacity computation need one human read of a real capture.",
        "verified": "VALUES HUMAN-READ 2026-07-25: see rows[].prefix_len."
      },
      {
        "path": "rows[].%subnet",
        "opaque": true,
        "metrics": [
          "opnsense_kea_dhcp6_pd_pool_size"
        ],
        "exercise": "Same PD pool as the already-ledgered rows[].prefix - the row's mere existence makes the key present. Exercising the CODE path behind it is a second, harder thing: pdPoolSubnetLabel prefers the rows[].subnet UUID join against keaSubnets6 and only falls back to this display string when that join misses, so reaching the fallback needs a PD pool whose subnet reference is stale (delete the parent subnet6 record, or point the pool at a UUID that no longer exists). Worth doing once by hand rather than leaving to chance: this is a \"<interface-description> <cidr>\" string and the label takes only the token after the last space, so a format change silently mislabels rather than empties.",
        "verified": "LIVE-VERIFIED 2026-07-31 on BOTH testbeds, replacing a blocker that named the wrong box. The empty rows array it was written from came from the PROD canary profile (#531): the production firewall runs kea-dhcp6 but its generated kea-dhcp6.conf carries \"pd-pools\": [] on every subnet, so searchPdPool is permanently empty there and no testbed change can affect it. Direct check 2026-07-31: both testbed VMs have populated pd-pools blocks in kea-dhcp6.conf, and the same day's nightly and release-vm canary reports (#608 / #607) carry NO required-coverage section for any endpoint. flexString (KindAny), so even a populated run can only ever prove the key exists."
      }
    ],
    "note": "PROD ADDENDUM 2026-07-31 (#531): the prod canary profile can never verify rows[].prefix/subnet and this is NOT a testbed task. The production firewall runs kea-dhcp6 but every subnet in its generated kea-dhcp6.conf carries \"pd-pools\": [], so searchPdPool answers with an empty rows array on every prod run. Adding a PD pool there is a config write on a live box and prod is read-only. Both testbeds have populated pd-pools blocks (live-checked 2026-07-31), which is why neither testbed canary issue (#607 / #608) reports these paths. See the wireguardClients note for why no exemption can suppress a required-coverage warning and what the real fix (per-profile coverage scoping) would be."
  },
  "netbirdStatus": {
    "required": [
      {
        "path": "peers.details[].fqdn",
        "metrics": [
          "opnsense_netbird_peer_connected",
          "opnsense_netbird_peer_direct",
          "opnsense_netbird_peer_received_bytes_total",
          "opnsense_netbird_peer_transmitted_bytes_total",
          "opnsense_netbird_peer_last_handshake_timestamp_seconds",
          "opnsense_netbird_peer_latency_seconds"
        ],
        "exercise": "Requires the dev box enrolled into a NetBird tenant AND at least one OTHER peer visible to it \u2014 a peer never lists itself, so a single-peer tenant yields peers.total 0 and an empty details[]. A second netstack-mode peer runs on the dev box for this purpose (NB_USE_NETSTACK_MODE=true, state dir /var/db/netbird-canary2, its own daemon socket), which creates no host interface or routes. It is started BY HAND and does NOT survive a reboot: if these paths regress to unverified, restart that second peer before treating it as drift.",
        "verified": "2026-07-25, live on the dev box in both the Connected and lazy-connection Idle states. The peer label on every netbird_peer_* series comes from this field. NOT REBOOT-DURABLE: the second peer that makes details[] non-empty is hand-started, so if these paths regress to unverified, restart it before treating it as drift - see the exercise recipe."
      },
      {
        "path": "peers.details[].status",
        "metrics": [
          "opnsense_netbird_peer_connected"
        ],
        "exercise": "Same enrolled + second-peer state as fqdn.",
        "verified": "2026-07-25, live in both Connected and Idle states; the value spelling matches the \"Connected\" comparison in netbird.go."
      },
      {
        "path": "peers.details[].connectionType",
        "metrics": [
          "opnsense_netbird_peer_direct"
        ],
        "exercise": "Same enrolled + second-peer state as fqdn. Both a relayed and a direct peer are needed to observe both branches of the direct/relayed mapping.",
        "verified": "2026-07-25, live in both Connected and Idle states."
      },
      {
        "path": "peers.details[].lastWireguardHandshake",
        "metrics": [
          "opnsense_netbird_peer_last_handshake_timestamp_seconds"
        ],
        "exercise": "Same enrolled + second-peer state as fqdn.",
        "verified": "2026-07-25, live in both Connected and Idle states. The RFC3339Nano parse plus the !t.IsZero() guard in netbird.go both behaved as the source-derived reading predicted."
      },
      {
        "path": "peers.details[].transferReceived",
        "metrics": [
          "opnsense_netbird_peer_received_bytes_total"
        ],
        "exercise": "Same enrolled + second-peer state as fqdn, with traffic over the tunnel so the counter is non-zero.",
        "verified": "2026-07-25, live in both Connected and Idle states."
      },
      {
        "path": "peers.details[].transferSent",
        "metrics": [
          "opnsense_netbird_peer_transmitted_bytes_total"
        ],
        "exercise": "As for transferReceived.",
        "verified": "2026-07-25, live in both Connected and Idle states."
      },
      {
        "path": "peers.details[].latency",
        "metrics": [
          "opnsense_netbird_peer_latency_seconds"
        ],
        "exercise": "Same enrolled + second-peer state as fqdn.",
        "verified": "2026-07-25, live in both Connected and Idle states; the wire value is a plain integer nanosecond count, as netbird's uninstrumented time.Duration implied."
      }
    ],
    "note": "RESOLVED 2026-07-25. Every field of netbirdPeerDetail was source-derived from netbirdio/netbird client/status/status.go and unobserved (#211); the dev box is now enrolled and all 22 paths in the netbirdStatus golden validated live (22 verified, 0 mismatch, 0 unverified) in both the Connected and Idle states, so every source-derived guess proved correct. The entries stay in the ledger as required BECAUSE the enrollment is not reboot-durable \u2014 a regression to unverified must read as \"restart the second canary peer\", not as drift. Never record the setup key, API token, tenant name, peer FQDNs, netbird IPs or public keys here: the recipe describes the required STATE only."
  },
  "ntpGPS": {
    "stateOptional": [
      {
        "path": "gps",
        "opaque": true,
        "reason": "GPS/PPS receiver state. api/ntpd/service/gps only reports a fix when a GNSS receiver is attached to a serial or USB port; the canary VM has none, so the block is empty or null.",
        "pruneTrigger": "delete when a canary box has a GNSS receiver attached"
      }
    ]
  },
  "quaggaOspfv3Interface": {
    "required": [
      {
        "path": "response.*.status",
        "metrics": [
          "opnsense_frr_ospfv3_interface_up"
        ],
        "exercise": "Bring up an OSPFv3 adjacency on a box with a working IPv6 stack (two os-frr peers over a v6 transit link), capture api/quagga/diagnostics/ospfv3interface, and check the field names against opnsense/frr.go's ospf6 structs by hand.",
        "verified": "LIVE STATE ACHIEVED 2026-07-25 (#377): OSPFv3 was enabled on the testbed and a Full adjacency brought up across the isolated bridge; all four v3 endpoints now return real objects instead of {\"response\":[]}, and all 13 source-derived field names in opnsense/frr.go were confirmed present in the live payload by hand. UNTIL #459, the canary still could not verify any of them structurally: schema_registry.go registered the bare ENVELOPE type, whose sole field is Response json.RawMessage -> KindAny. #459 (envelopeDescent) unwraps the envelope, so this is now an ORDINARY required path \u2014 the same live run that achieved 2026-07-25 state would resolve it, once re-run."
      },
      {
        "path": "response.*.cost",
        "metrics": [
          "opnsense_frr_ospfv3_interface_cost"
        ],
        "exercise": "Same OSPFv3 lab as above.",
        "verified": "See response.*.status above \u2014 same 2026-07-25 live confirmation, same #459 unwrap."
      },
      {
        "path": "response.*.ospf6InterfaceState",
        "metrics": [
          "opnsense_frr_ospfv3_interface_state"
        ],
        "exercise": "Same OSPFv3 lab as above; the adjacency-bearing interface returns a real non-Down value (BDR was observed).",
        "verified": "See response.*.status above \u2014 same 2026-07-25 live confirmation, same #459 unwrap."
      },
      {
        "path": "response.*.pendingLsaLsUpdateCount",
        "metrics": [
          "opnsense_frr_ospfv3_interface_pending_lsa"
        ],
        "exercise": "Same OSPFv3 lab as above.",
        "verified": "See response.*.status above \u2014 same 2026-07-25 live confirmation, same #459 unwrap."
      },
      {
        "path": "response.*.pendingLsaLsAckCount",
        "metrics": [
          "opnsense_frr_ospfv3_interface_pending_lsa"
        ],
        "exercise": "Same OSPFv3 lab as above.",
        "verified": "See response.*.status above \u2014 same 2026-07-25 live confirmation, same #459 unwrap."
      }
    ],
    "note": "Until #459 the only path OSPFv3 exposed to the canary was the response envelope, recorded as required-but-opaque so the gap was a NAMED blind spot instead of vanishing into the unverified count (see the OSPFv3 note in the VERIFICATION comment at the top of opnsense/frr.go). #459 registered an envelopeDescent for this endpoint instead, so the schema now carries the real inner paths and these are ordinary required entries a live run resolves."
  },
  "quaggaOspfv3Overview": {
    "required": [
      {
        "path": "response.areas.*.numberOfAreaScopedLsa",
        "metrics": [
          "opnsense_frr_ospfv3_area_lsa_count"
        ],
        "exercise": "Same OSPFv3 lab as quaggaOspfv3Interface; capture api/quagga/diagnostics/ospfv3overview with at least one area carrying LSAs and check the area key structure by hand.",
        "verified": "LIVE STATE ACHIEVED 2026-07-25 (#377): OSPFv3 was enabled on the testbed and a Full adjacency brought up across the isolated bridge; all four v3 endpoints now return real objects instead of {\"response\":[]}, and all 13 source-derived field names in opnsense/frr.go were confirmed present in the live payload by hand. UNTIL #459, the canary still could not verify this structurally: schema_registry.go registered the bare ENVELOPE type, whose sole field is Response json.RawMessage -> KindAny. #459 (envelopeDescent) unwraps the envelope, so this is now an ORDINARY required path \u2014 the same live run that achieved 2026-07-25 state would resolve it, once re-run."
      }
    ],
    "note": "Until #459 the only path OSPFv3 exposed to the canary was the response envelope, recorded as required-but-opaque (see the OSPFv3 note in the VERIFICATION comment at the top of opnsense/frr.go). #459 registered an envelopeDescent for this endpoint instead, so the schema now carries the real inner path and this is an ordinary required entry a live run resolves."
  },
  "systemTemperature": {
    "stateOptional": [
      {
        "path": "[].*",
        "reason": "Thermal sensors. api/diagnostics/system/systemTemperature returns [] on a box with no coretemp/amdtemp/acpi_thermal source, which is every virtualised canary; the whole element subtree is therefore unverified.",
        "pruneTrigger": "delete when the canary runs on real hardware, or when the hypervisor passes a thermal zone through to the guest"
      }
    ]
  },
  "hasyncServices": {
    "required": [
      {
        "path": "rows[].name",
        "opaque": true,
        "metrics": [
          "opnsense_hasync_remote_service_running",
          "opnsense_hasync_remote_services"
        ],
        "exercise": "Two OPNsense boxes on the isolated testbed bridge with hasync pointed at each other (System > High Availability > Settings: synchronize-to-IP = the peer, plus a shared XML-RPC account holding page-all on BOTH nodes - the account is authenticated on the box being CALLED, so it has to exist on both or the endpoint answers {\"response\":\"Authentication failed\"} rather than throwing). This endpoint serves the CACHED remote service list, so it stays an empty bootgrid ({\"total\":0,\"rows\":[]}) until a peer is configured and has been contacted at least once. With a peer up it returns one row per service running on the OTHER node.",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460): 38 rows seen from one box and 48 from the other - the lists are legitimately different lengths because the two nodes run different plugin sets and different releases. First time this endpoint has ever returned a non-empty rows array on the testbed, so nothing under rows[] had previously been checked by the canary at all."
      },
      {
        "path": "rows[].status",
        "opaque": true,
        "metrics": [
          "opnsense_hasync_remote_service_running"
        ],
        "exercise": "Same HA pair as rows[].name. Both values occur naturally - a normal box has a handful of stopped services alongside the running ones - so no extra step is needed to exercise both branches of the running/stopped mapping.",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460): arrives as a NATIVE JSON boolean, not a string, with both true and false present in the same payload. That is exactly what flexBool was written to tolerate, and it is now proven rather than source-derived."
      },
      {
        "path": "rows[].id",
        "opaque": true,
        "metrics": [
          "opnsense_hasync_remote_service_running"
        ],
        "exercise": "Same HA pair as rows[].name, PLUS at least one multi-instance service on the peer - an OpenVPN instance, a dpinger gateway monitor, or the Kea control agent. id is absent on single-instance rows, so a peer running only single-instance services leaves this path unverified even with a fully populated endpoint.",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460): present on 6 of 38 rows - two OpenVPN instances keyed by config UUID, three dpinger rows keyed by gateway name, and the Kea control agent. It is the id label on opnsense_hasync_remote_service_running, and the real values are UUIDs and names, NOT the small integer the previous synthetic fixture implied."
      }
    ],
    "note": "REACHED 2026-07-28 by #460, which built the second OPNsense this endpoint always needed (a clone of the devel box attached to the isolated host-internal bridge only, stripped of the cloned production-adjacent config and given its own identity and API credentials). rows[].description is decoded into hasyncServiceRow but backs no metric, so it is deliberately NOT ledgered as required - it is present on every row and would only add an entry nothing consumes. REGRESSION TRIGGER: the list is a CACHE of the peer's services, so stopping the second VM leaves these paths unverified rather than missing. KNOWN OPEN, and NOT drift - do not lab-chase either of these: with the endpoint finally populated the canary now reports (a) rows[].running as a MISSING path and (b) eight unmodelled nested keys (configd, dns_ports, locked, mwexec, nocheck, php, pidfile, uid). rows[].running is a DEFENSIVE ALIAS in hasyncServiceRow that upstream has never emitted on any release - list_services_xmlrpc sets 'status' only - so it can never be present on any box. RESOLVED - that missingOK entry already EXISTS in exemptions.json (hasyncServices.missingOK = [\"rows[].running\"]), verified 2026-07-31, so this is NOT an open item and #599's hand-off asking for one was a no-op. The 'wants a missingOK' wording above was written 2026-07-28 before the entry landed and was read as still-open three days later - a stale TODO in a ledger note is worse than none, because the next reader spends the trip to exemptions.json to find out. Not a testbed change either way. The eight extras are #457 baseline material. AUDIT ADDENDUM 2026-07-31 (#599): both remaining unclassified paths here stay informational, and for DIFFERENT reasons. rows[].description is a dead field (cmd/fieldaudit exempt, as the note above already says). rows[].running IS read - hasync.go resolves service state as `row.Status.Bool() || row.Running.Bool()`, so it would back opnsense_hasync_remote_service_running if it ever arrived - but upstream's list_services_xmlrpc sets 'status' only and has never emitted it on any release, so a required entry here would be a warning no live run and no testbed change could ever clear, which is the permanent noise this ledger exists to avoid. Its sibling rows[].status is already ledgered required and carries the same metric. STILL OPEN and unchanged by #599: the correct home for it is a missingOK entry in exemptions.json, not this file."
  },
  "carpStatus": {
    "required": [
      {
        "path": "rows[].status",
        "metrics": [
          "opnsense_carp_vip_status"
        ],
        "exercise": "Two OPNsense boxes sharing one L2 segment (the isolated testbed bridge), each carrying the SAME CARP VIP - same address, same vhid, same password - with DIFFERENT advskew so the split is deterministic rather than a race: the lower skew wins MASTER, the higher sits BACKUP. Flip them with POST api/diagnostics/interface/carp_status/maintenance on the master; it TOGGLES, so posting it once enters maintenance and fails over, and posting it again leaves maintenance and fails back. A single-node box can only ever report MASTER, so the BACKUP branch of parseCARPStatus needs the second box.",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460): MASTER on one box and BACKUP on the other simultaneously, swapped across a maintenance-mode failover, then swapped back on failback - so opnsense_carp_vip_status was observed as both 1 and 0 on both boxes. INIT (2) was NOT observed and is not ledgered: it is a sub-second transient a poll-interval scrape is unlikely to ever catch."
      },
      {
        "path": "rows[].advskew",
        "metrics": [
          "opnsense_carp_vip_advskew"
        ],
        "exercise": "Same two-box CARP pair. A non-zero advskew is what makes the MASTER/BACKUP split deterministic, so this path only becomes meaningful once a second box exists - on a single node it is pinned at 0, where a parse bug and a missing field look identical.",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460): 0 on the master and a large non-zero skew on the backup, both served as strings."
      },
      {
        "path": "rows[].advbase",
        "metrics": [
          "opnsense_carp_vip_advbase_seconds"
        ],
        "exercise": "Same two-box CARP pair; the default advertisement base of one second is served as a string.",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460) on both boxes in every state."
      },
      {
        "path": "rows[].interface",
        "metrics": [
          "opnsense_carp_vip_status",
          "opnsense_carp_vip_advbase_seconds",
          "opnsense_carp_vip_advskew"
        ],
        "exercise": "Same two-box CARP pair. This carries the interface DESCRIPTION, not the config key and not the device name - it is the interface label on every carp_vip_* series.",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460): the two boxes reach the shared segment on different config keys and different device names, yet both serve the same description here, which confirms the label is stable across an HA pair rather than per-box."
      },
      {
        "path": "rows[].vhid",
        "metrics": [
          "opnsense_carp_vip_status",
          "opnsense_carp_vip_advbase_seconds",
          "opnsense_carp_vip_advskew"
        ],
        "exercise": "Same two-box CARP pair; both nodes must advertise the same vhid or they never contend for the VIP at all.",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460): identical on both boxes, served as a string."
      },
      {
        "path": "rows[].subnet",
        "metrics": [
          "opnsense_carp_vip_status",
          "opnsense_carp_vip_advbase_seconds",
          "opnsense_carp_vip_advskew"
        ],
        "exercise": "Same two-box CARP pair. carp.go reads THIS field as the vip label because get_vip_status has no \"vip\" key at all, so a rename here silently empties the label instead of failing the scrape.",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460): identical on both boxes and equal to the shared VIP address."
      },
      {
        "path": "carp.demotion",
        "metrics": [
          "opnsense_carp_demotion"
        ],
        "exercise": "Enter CARP maintenance mode on one box with POST api/diagnostics/interface/carp_status/maintenance. A healthy box reports \"0\"; the demoted box reports a large value. Toggling maintenance off returns it to \"0\".",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460): \"0\" -> \"240\" -> \"0\" across a maintenance-mode failover and failback, so opnsense_carp_demotion carried a non-zero value for the first time. The peer's demotion stayed \"0\" throughout - demotion is local, not synchronised."
      },
      {
        "path": "carp.maintenancemode",
        "metrics": [
          "opnsense_carp_maintenance_mode"
        ],
        "exercise": "Same maintenance toggle as carp.demotion. Note the endpoint TOGGLES rather than sets: the response says enter_maintenance the first time and leave_maintenance the second, so a script that posts it to 'ensure' maintenance will silently do the opposite on the second run.",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460): observed as both false and true, as a NATIVE JSON boolean, so opnsense_carp_maintenance_mode was seen as both 0 and 1."
      },
      {
        "path": "carp.allow",
        "metrics": [
          "opnsense_carp_allow"
        ],
        "exercise": "Present on any box with CARP configured; served as a numeric string rather than a boolean.",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460): \"1\" on both boxes in every state INCLUDING while demoted - entering maintenance does not clear allow, so a dashboard cannot use carp_allow as a proxy for 'this node is serving'."
      }
    ],
    "note": "REACHED 2026-07-28 by #460. The endpoint was always populated on the single-node testbed - one CARP VIP existed - but every interesting VALUE was pinned: status could only be MASTER, advskew could only be 0, demotion could only be 0 and maintenancemode could only be false. The second OPNsense makes all four vary, which is why these are ledgered now rather than when the endpoint first answered. LIVE ANSWER to the rows[].mode question the carpStatus note in exemptions.json parks for #457: mode is served as the string \"carp\" and is REDUNDANT, not a VIP-type discriminator. get_vip_status returns vhid-bearing CARP VIPs only - the devel box also carries an ipalias VIP on the loopback and it does NOT appear in rows at all - so mode is constant for every row this endpoint can ever emit, and belongs in carpStatus.knownExtraPaths beside vhid_txt. SECOND CORRECTION to that same note: it claims carp.status_msg \"only appears in the 'no CARP interfaces configured' case\". That is WRONG. status_msg was present as an empty string in all twelve captures taken across MASTER, BACKUP, maintenance and failback on two boxes that each had a configured VIP. It is always present, and merely empty when there is nothing to say. REGRESSION TRIGGER: stop the second VM and status pins back to MASTER and advskew to 0 on the survivor. The paths stay verified but stop VARYING, which no canary can detect - only this note. AUDIT ADDENDUM 2026-07-31 (#599): rows[].status_txt is deliberately NOT ledgered. It is decoded and never read - cmd/fieldaudit lists carpVIPRow.StatusTxt as an exempt dead field, localised presentation copy of the rows[].status this endpoint already exports - so it backs no metric. rows[] itself is the element container and is left informational alongside the nine children above. PROD ADDENDUM 2026-07-31 (#531): the prod canary profile can never verify any of these paths and this is NOT a testbed task. The production firewall carries ZERO CARP VIPs (no <vhid> element in its config, net.inet.carp.demotion 0) and has no HA peer, so get_vip_status answers with an empty rows array on every prod run - which is why #531 lists rows[].advbase/advskew/interface/status/subnet/vhid as required-coverage warnings while both testbed canary issues (#607 / #608) list none. Configuring CARP on prod would need a second production firewall AND config writes on a live box; prod is read-only. See the wireguardClients note for why no exemption can suppress a required-coverage warning and what the real fix (per-profile coverage scoping) would be."
  },
  "keaLeases4": {
    "required": [
      {
        "path": "rows[].address",
        "metrics": [
          "opnsense_kea_dhcp4_lease_info",
          "opnsense_kea_dhcp4_leases",
          "opnsense_kea_dhcp4_leases_by_interface",
          "opnsense_kea_dhcp4_pool_used"
        ],
        "exercise": "A DHCPv4 client on a Kea-served testbed VLAN. The traffgen LXC on the isolated bridge is that client: its primary interface is configured for DHCP and a Kea host RESERVATION pins it to the address the IPsec test peers hardcode, so the lease is deterministic and turning DHCP on does not break the tunnel.",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460): kea/leases4/search returns a populated rows array for the first time. This path had been recorded as empty since 2026-07-13."
      },
      {
        "path": "rows[].hwaddr",
        "metrics": [
          "opnsense_kea_dhcp4_lease_info"
        ],
        "exercise": "Same as rows[].address.",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460)."
      },
      {
        "path": "rows[].hostname",
        "metrics": [
          "opnsense_kea_dhcp4_lease_info"
        ],
        "exercise": "Same as rows[].address, and the client must send a hostname option or the reservation must carry one - a bare DHCP client with no hostname leaves this an empty string.",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460): populated from the host reservation."
      },
      {
        "path": "rows[].state",
        "opaque": true,
        "metrics": [
          "opnsense_kea_dhcp4_leases_by_state"
        ],
        "exercise": "Same as rows[].address. Only the default/active state occurs with a single well-behaved client; the declined and expired states would need a deliberately misbehaving one.",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460): arrives as a NUMBER, not a numeric string. Remains opaque because the field is a flex type, so this note is the evidence, not a canary assertion."
      },
      {
        "path": "rows[].is_reserved",
        "opaque": true,
        "metrics": [
          "opnsense_kea_dhcp4_leases_reserved",
          "opnsense_kea_dhcp4_leases_dynamic"
        ],
        "exercise": "Same as rows[].address. Note the traffgen lease IS reserved, so it exercises the reserved branch only; the dynamic branch of the reserved/dynamic split still needs a second, unreserved DHCPv4 client.",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460): served as an ARRAY naming the matching criteria, NOT a boolean - so a reader that treats it as a bool would be wrong even though it reads truthy."
      },
      {
        "path": "rows[].if_descr",
        "metrics": [
          "opnsense_kea_dhcp4_leases_by_interface"
        ],
        "exercise": "Same as rows[].address.",
        "verified": "LIVE-VERIFIED 2026-07-28 (#460): the interface description, matching the label carp.go and kea.go both use."
      },
      {
        "path": "rows[].expire",
        "opaque": true,
        "metrics": [
          "opnsense_kea_dhcp4_lease_info"
        ],
        "exercise": "Same as the already-ledgered rows[].address: the traffgen LXC's DHCPv4 lease on the Kea-served testbed VLAN. All four of these additional paths sit on the details-gated opnsense_kea_dhcp4_lease_info series, so the canary needs the lease AND the exporter needs --exporter.enable-kea-details for the metric to exist at all. expire is the VALUE of lease_info (every other field on it is a label), so it is the one path here whose drift changes a number rather than a label.",
        "verified": "LIVE-VERIFIED 2026-07-31 on BOTH testbeds, replacing a blocker that named the wrong box. The empty rows array it was written from came from the PROD canary profile (#531): the production firewall serves DHCPv4 from dnsmasq, has no Kea DHCPv4 subnet configured and does not run kea-dhcp4 at all (only kea-dhcp6), so its lease list is permanently empty and no testbed change can affect it. The misattribution came from camden's prod canary stamping `--generation \"release <version>\"`, which reads as the release-channel testbed but means the production firewall's release channel. Direct check 2026-07-31: both testbed VMs hold Kea DHCPv4 leases on disk (kea-leases4.csv, allowing for Kea's LFC rotation into the .2 file), and the same day's nightly and release-vm canary reports (#608 / #607) carry NO required-coverage section for any endpoint. Check that kea-dhcp4 and not dnsmasq or the legacy dhcpd owns UDP/67 before calling an empty list drift on a TESTBED - that collision has broken this twice (#460, #502). flexInt (KindAny), so a populated run can only ever prove the key exists; note it is Kea's cltt+valid-lft sum and cltt is never exposed, so valid_lifetime cannot be re-derived from it."
      },
      {
        "path": "rows[].valid_lifetime",
        "opaque": true,
        "metrics": [
          "opnsense_kea_dhcp4_lease_info"
        ],
        "exercise": "Same as the already-ledgered rows[].address: the traffgen LXC's DHCPv4 lease on the Kea-served testbed VLAN. All four of these additional paths sit on the details-gated opnsense_kea_dhcp4_lease_info series, so the canary needs the lease AND the exporter needs --exporter.enable-kea-details for the metric to exist at all. Kea's valid-lft, stringified into a label. Not derivable from expire (see that entry), which is why it is modelled separately.",
        "verified": "LIVE-VERIFIED 2026-07-31 on BOTH testbeds, replacing a blocker that named the wrong box. The empty rows array it was written from came from the PROD canary profile (#531): the production firewall serves DHCPv4 from dnsmasq, has no Kea DHCPv4 subnet configured and does not run kea-dhcp4 at all (only kea-dhcp6), so its lease list is permanently empty and no testbed change can affect it. The misattribution came from camden's prod canary stamping `--generation \"release <version>\"`, which reads as the release-channel testbed but means the production firewall's release channel. Direct check 2026-07-31: both testbed VMs hold Kea DHCPv4 leases on disk (kea-leases4.csv, allowing for Kea's LFC rotation into the .2 file), and the same day's nightly and release-vm canary reports (#608 / #607) carry NO required-coverage section for any endpoint. Check that kea-dhcp4 and not dnsmasq or the legacy dhcpd owns UDP/67 before calling an empty list drift on a TESTBED - that collision has broken this twice (#460, #502). flexInt (KindAny), so a populated run can only ever prove the key exists."
      },
      {
        "path": "rows[].client_id",
        "metrics": [
          "opnsense_kea_dhcp4_lease_info"
        ],
        "exercise": "Same as the already-ledgered rows[].address: the traffgen LXC's DHCPv4 lease on the Kea-served testbed VLAN. All four of these additional paths sit on the details-gated opnsense_kea_dhcp4_lease_info series, so the canary needs the lease AND the exporter needs --exporter.enable-kea-details for the metric to exist at all. The client must actually send DHCP option 61: dhclient does by default, systemd-networkd with ClientIdentifier=mac does NOT, and Kea's lease6 records have no client-id concept at all, which is why this label is added on the v4 call site only (includeClientID). A client that omits option 61 leaves it an empty string, which is indistinguishable from a rename.",
        "verified": "LIVE-VERIFIED 2026-07-31 on BOTH testbeds, replacing a blocker that named the wrong box. The empty rows array it was written from came from the PROD canary profile (#531): the production firewall serves DHCPv4 from dnsmasq, has no Kea DHCPv4 subnet configured and does not run kea-dhcp4 at all (only kea-dhcp6), so its lease list is permanently empty and no testbed change can affect it. The misattribution came from camden's prod canary stamping `--generation \"release <version>\"`, which reads as the release-channel testbed but means the production firewall's release channel. Direct check 2026-07-31: both testbed VMs hold Kea DHCPv4 leases on disk (kea-leases4.csv, allowing for Kea's LFC rotation into the .2 file), and the same day's nightly and release-vm canary reports (#608 / #607) carry NO required-coverage section for any endpoint. Check that kea-dhcp4 and not dnsmasq or the legacy dhcpd owns UDP/67 before calling an empty list drift on a TESTBED - that collision has broken this twice (#460, #502)."
      },
      {
        "path": "rows[].mac_info",
        "metrics": [
          "opnsense_kea_dhcp4_lease_info"
        ],
        "exercise": "Same as the already-ledgered rows[].address: the traffgen LXC's DHCPv4 lease on the Kea-served testbed VLAN. All four of these additional paths sit on the details-gated opnsense_kea_dhcp4_lease_info series, so the canary needs the lease AND the exporter needs --exporter.enable-kea-details for the metric to exist at all. This is PHP-side enrichment, not wire data: LeasesController.php looks the first three hwaddr octets up in the OPNsense macdb and returns a vendor NAME. It is empty whenever the OUI is unknown, and a Proxmox LXC's veth gets a random locally-administered address by default, so the traffgen LXC's MAC must be pinned to a real vendor OUI or this path stays empty even with a healthy lease.",
        "verified": "LIVE-VERIFIED 2026-07-31 on BOTH testbeds, replacing a blocker that named the wrong box. The empty rows array it was written from came from the PROD canary profile (#531): the production firewall serves DHCPv4 from dnsmasq, has no Kea DHCPv4 subnet configured and does not run kea-dhcp4 at all (only kea-dhcp6), so its lease list is permanently empty and no testbed change can affect it. The misattribution came from camden's prod canary stamping `--generation \"release <version>\"`, which reads as the release-channel testbed but means the production firewall's release channel. Direct check 2026-07-31: both testbed VMs hold Kea DHCPv4 leases on disk (kea-leases4.csv, allowing for Kea's LFC rotation into the .2 file), and the same day's nightly and release-vm canary reports (#608 / #607) carry NO required-coverage section for any endpoint. Check that kea-dhcp4 and not dnsmasq or the legacy dhcpd owns UDP/67 before calling an empty list drift on a TESTBED - that collision has broken this twice (#460, #502)."
      }
    ],
    "note": "CLOSED 2026-07-28 by #460. ROOT CAUSE of the long-standing empty lease list, recorded so nobody re-investigates it: the traffgen LXC held a STATIC address, so nothing on the Kea-served VLAN ever asked for a lease. Switching it to DHCP was NOT sufficient on its own - the legacy ISC dhcpd had restarted and was wildcard-binding UDP/67, silently starving kea-dhcp4 of its socket, so DHCPDISCOVER drew no offers at all. Stopping dhcpd and restarting Kea produced an offer immediately. This is the same dhcpd-versus-Kea collision recorded on 2026-07-13, and it recurs because dhcpd is only STOPPED, never disabled in config. REGRESSION TRIGGERS, likeliest first: (1) the firewall reboots and brings dhcpd back, re-breaking Kea DHCPv4 - check that kea-dhcp4 and not dhcpd owns UDP/67 before calling this drift; (2) the lease expires if the traffgen LXC stops renewing. AUDIT ADDENDUM 2026-07-31 (#599): rows[].type is deliberately NOT ledgered. It is decoded and read, but Kea has no lease-type concept on v4 so it is permanently the empty string, fetchKeaLeases excludes empty types from LeasesByType, and the v4 call site passes nil for the leases_by_type descriptor - so no v4 series can ever depend on it. Its v6 twin IS ledgered on keaLeases6. rows[] itself is the element container and is left informational. PROD ADDENDUM 2026-07-31 (#531): the prod canary profile can never verify these paths and this is NOT a testbed task. The production firewall serves DHCPv4 from dnsmasq, has no Kea DHCPv4 subnet configured and does not run kea-dhcp4 at all, so kea/leases4/search is permanently empty there. Migrating prod's DHCPv4 to Kea is a live-service change and prod is read-only. See the wireguardClients note for why no exemption can suppress a required-coverage warning."
  },
  "wireguardClients": {
    "required": [
      {
        "path": "rows[].latest-handshake",
        "metrics": [
          "opnsense_wireguard_peer_last_handshake_timestamp_seconds",
          "opnsense_wireguard_peer_handshake_age_seconds"
        ],
        "exercise": "Enable WireGuard on the box (Services > WireGuard > general, enabled=1). Both testbeds carry the same self-contained pair from the #460 clone - two local instances wgtest1/wgtest2 peering each other over the loopback with a short persistent-keepalive - so no external peer is needed and a handshake re-establishes within seconds of enabling. These three keys exist ONLY on type=\"peer\" rows. CORRECTION 2026-07-31: an earlier version of this recipe claimed a box with WireGuard DISABLED still returns its type=\"interface\" rows, so that an EMPTY rows array would prove the instances had been deleted rather than merely switched off. That is WRONG and the distinction does not exist. Upstream builds every record from `wg show all dump` (/usr/local/opnsense/scripts/wireguard/wg_show.py), so a stopped instance has no kernel wg interface and contributes NO row of either type - a disabled box and a never-configured box both answer with an empty rows array. An empty rows array therefore says nothing about which of the two it is; only looking at the box does.",
        "verified": "LIVE-VERIFIED 2026-07-29 on both testbeds (#502). VM 102 had WireGuard enabled and served peer rows; VM 106 was enabled=0 from the clone strip and reported all three paths missing in the first release-vm canary run (#508). Enabling it on 106 produced peer rows carrying all three within 15s. Fixed on the box rather than exempted, per the triage rule that a field backing an exported metric should have the testbed fixed instead \u2014 an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].transfer-rx",
        "metrics": [
          "opnsense_wireguard_peer_received_bytes_total"
        ],
        "exercise": "Same as rows[].latest-handshake; the keepalive alone moves enough bytes for both byte counters to be non-zero.",
        "verified": "LIVE-VERIFIED 2026-07-29 on both testbeds (#502). VM 102 had WireGuard enabled and served peer rows; VM 106 was enabled=0 from the clone strip and reported all three paths missing in the first release-vm canary run (#508). Enabling it on 106 produced peer rows carrying all three within 15s. Fixed on the box rather than exempted, per the triage rule that a field backing an exported metric should have the testbed fixed instead \u2014 an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].transfer-tx",
        "metrics": [
          "opnsense_wireguard_peer_transmitted_bytes_total"
        ],
        "exercise": "Same as rows[].transfer-rx.",
        "verified": "LIVE-VERIFIED 2026-07-29 on both testbeds (#502). VM 102 had WireGuard enabled and served peer rows; VM 106 was enabled=0 from the clone strip and reported all three paths missing in the first release-vm canary run (#508). Enabling it on 106 produced peer rows carrying all three within 15s. Fixed on the box rather than exempted, per the triage rule that a field backing an exported metric should have the testbed fixed instead \u2014 an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].type",
        "metrics": [
          "opnsense_wireguard_interfaces_status",
          "opnsense_wireguard_peer_status",
          "opnsense_wireguard_peer_received_bytes_total",
          "opnsense_wireguard_peer_transmitted_bytes_total",
          "opnsense_wireguard_peer_last_handshake_timestamp_seconds",
          "opnsense_wireguard_peer_handshake_age_seconds"
        ],
        "exercise": "The type=\"interface\" rows, which the box serves for every configured WireGuard instance. The wgtest1/wgtest2 pair from the #460 clone (two local instances peering each other over the loopback with a short persistent-keepalive) is what provides them on both testbeds, so this resolves together with the already-ledgered latest-handshake/transfer-rx/transfer-tx paths. type is the device_type label on every series AND the interface/peer discriminator: a row is sorted into the interface or the peer list purely on this field, and a row matching neither is dropped. A rename therefore empties every WireGuard series at once.",
        "verified": "LIVE-VERIFIED 2026-07-31 on BOTH testbeds, replacing a blocker that named the wrong box. The empty rows array that blocker was written from came from the PROD canary profile (#531), not from a testbed: camden's prod canary stamps its report `--generation \"release <version>\"` (opnsense-prod-canary.sh), and \"release\" there means the production firewall's release CHANNEL, not the release-channel testbed VM - reading it as the testbed is how prod box state got filed as a testbed regression. Direct check on both testbed VMs: `configctl wireguard show` returns the wgtest loopback pair with type=\"interface\" rows up and type=\"peer\" rows carrying a fresh handshake and non-zero transfer counters in both directions; name/ifname/peer-status are added on top of those records by the API controller (/usr/local/opnsense/mvc/app/controllers/OPNsense/Wireguard/Api/ServiceController.php, lines 90-117) and are therefore present on every row the script emitted. The same day's nightly and release-vm canary reports (#608 / #607) carry NO required-coverage section at all for any endpoint, which is the canary's own confirmation that all nine of this endpoint's paths resolve on both boxes."
      },
      {
        "path": "rows[].if",
        "metrics": [
          "opnsense_wireguard_interfaces_status",
          "opnsense_wireguard_peer_status",
          "opnsense_wireguard_peer_received_bytes_total",
          "opnsense_wireguard_peer_transmitted_bytes_total",
          "opnsense_wireguard_peer_last_handshake_timestamp_seconds",
          "opnsense_wireguard_peer_handshake_age_seconds"
        ],
        "exercise": "The type=\"interface\" rows, which the box serves for every configured WireGuard instance. The wgtest1/wgtest2 pair from the #460 clone (two local instances peering each other over the loopback with a short persistent-keepalive) is what provides them on both testbeds, so this resolves together with the already-ledgered latest-handshake/transfer-rx/transfer-tx paths. The device label on every interface and peer series.",
        "verified": "LIVE-VERIFIED 2026-07-31 on BOTH testbeds, replacing a blocker that named the wrong box. The empty rows array that blocker was written from came from the PROD canary profile (#531), not from a testbed: camden's prod canary stamps its report `--generation \"release <version>\"` (opnsense-prod-canary.sh), and \"release\" there means the production firewall's release CHANNEL, not the release-channel testbed VM - reading it as the testbed is how prod box state got filed as a testbed regression. Direct check on both testbed VMs: `configctl wireguard show` returns the wgtest loopback pair with type=\"interface\" rows up and type=\"peer\" rows carrying a fresh handshake and non-zero transfer counters in both directions; name/ifname/peer-status are added on top of those records by the API controller (/usr/local/opnsense/mvc/app/controllers/OPNsense/Wireguard/Api/ServiceController.php, lines 90-117) and are therefore present on every row the script emitted. The same day's nightly and release-vm canary reports (#608 / #607) carry NO required-coverage section at all for any endpoint, which is the canary's own confirmation that all nine of this endpoint's paths resolve on both boxes."
      },
      {
        "path": "rows[].ifname",
        "metrics": [
          "opnsense_wireguard_interfaces_status",
          "opnsense_wireguard_peer_status",
          "opnsense_wireguard_peer_received_bytes_total",
          "opnsense_wireguard_peer_transmitted_bytes_total",
          "opnsense_wireguard_peer_last_handshake_timestamp_seconds",
          "opnsense_wireguard_peer_handshake_age_seconds"
        ],
        "exercise": "The type=\"interface\" rows, which the box serves for every configured WireGuard instance. The wgtest1/wgtest2 pair from the #460 clone (two local instances peering each other over the loopback with a short persistent-keepalive) is what provides them on both testbeds, so this resolves together with the already-ledgered latest-handshake/transfer-rx/transfer-tx paths. The device_name label on every interface and peer series.",
        "verified": "LIVE-VERIFIED 2026-07-31 on BOTH testbeds, replacing a blocker that named the wrong box. The empty rows array that blocker was written from came from the PROD canary profile (#531), not from a testbed: camden's prod canary stamps its report `--generation \"release <version>\"` (opnsense-prod-canary.sh), and \"release\" there means the production firewall's release CHANNEL, not the release-channel testbed VM - reading it as the testbed is how prod box state got filed as a testbed regression. Direct check on both testbed VMs: `configctl wireguard show` returns the wgtest loopback pair with type=\"interface\" rows up and type=\"peer\" rows carrying a fresh handshake and non-zero transfer counters in both directions; name/ifname/peer-status are added on top of those records by the API controller (/usr/local/opnsense/mvc/app/controllers/OPNsense/Wireguard/Api/ServiceController.php, lines 90-117) and are therefore present on every row the script emitted. The same day's nightly and release-vm canary reports (#608 / #607) carry NO required-coverage section at all for any endpoint, which is the canary's own confirmation that all nine of this endpoint's paths resolve on both boxes."
      },
      {
        "path": "rows[].status",
        "metrics": [
          "opnsense_wireguard_interfaces_status"
        ],
        "exercise": "The type=\"interface\" rows, which the box serves for every configured WireGuard instance. The wgtest1/wgtest2 pair from the #460 clone (two local instances peering each other over the loopback with a short persistent-keepalive) is what provides them on both testbeds, so this resolves together with the already-ledgered latest-handshake/transfer-rx/transfer-tx paths. This is the VALUE of the interfaces_status gauge, read only on interface rows, and it is a TRANSLATED string - parseWGInterfaceStatus matches \"up\"/\"down\" and logs a warning for anything else, reporting 2 (unknown). Both branches occur naturally: an instance that is configured but not enabled reports down.",
        "verified": "LIVE-VERIFIED 2026-07-31 on BOTH testbeds, replacing a blocker that named the wrong box. The empty rows array that blocker was written from came from the PROD canary profile (#531), not from a testbed: camden's prod canary stamps its report `--generation \"release <version>\"` (opnsense-prod-canary.sh), and \"release\" there means the production firewall's release CHANNEL, not the release-channel testbed VM - reading it as the testbed is how prod box state got filed as a testbed regression. Direct check on both testbed VMs: `configctl wireguard show` returns the wgtest loopback pair with type=\"interface\" rows up and type=\"peer\" rows carrying a fresh handshake and non-zero transfer counters in both directions; name/ifname/peer-status are added on top of those records by the API controller (/usr/local/opnsense/mvc/app/controllers/OPNsense/Wireguard/Api/ServiceController.php, lines 90-117) and are therefore present on every row the script emitted. The same day's nightly and release-vm canary reports (#608 / #607) carry NO required-coverage section at all for any endpoint, which is the canary's own confirmation that all nine of this endpoint's paths resolve on both boxes."
      },
      {
        "path": "rows[].name",
        "metrics": [
          "opnsense_wireguard_peer_status",
          "opnsense_wireguard_peer_received_bytes_total",
          "opnsense_wireguard_peer_transmitted_bytes_total",
          "opnsense_wireguard_peer_last_handshake_timestamp_seconds",
          "opnsense_wireguard_peer_handshake_age_seconds"
        ],
        "exercise": "The peer_name label on every peer series, from type=\"peer\" rows only. The wgtest1/wgtest2 loopback pair provides them. Note peer_name is free-form user config that OPNsense does not force to be unique per interface, so the collector skips a peer whose (device, device_type, device_name, peer_name) tuple repeats (#85) rather than failing the scrape - two identically named peers on one device silently lose one.",
        "verified": "LIVE-VERIFIED 2026-07-31 on BOTH testbeds, replacing a blocker that named the wrong box. The empty rows array that blocker was written from came from the PROD canary profile (#531), not from a testbed: camden's prod canary stamps its report `--generation \"release <version>\"` (opnsense-prod-canary.sh), and \"release\" there means the production firewall's release CHANNEL, not the release-channel testbed VM - reading it as the testbed is how prod box state got filed as a testbed regression. Direct check on both testbed VMs: `configctl wireguard show` returns the wgtest loopback pair with type=\"interface\" rows up and type=\"peer\" rows carrying a fresh handshake and non-zero transfer counters in both directions; name/ifname/peer-status are added on top of those records by the API controller (/usr/local/opnsense/mvc/app/controllers/OPNsense/Wireguard/Api/ServiceController.php, lines 90-117) and are therefore present on every row the script emitted. The same day's nightly and release-vm canary reports (#608 / #607) carry NO required-coverage section at all for any endpoint, which is the canary's own confirmation that all nine of this endpoint's paths resolve on both boxes."
      },
      {
        "path": "rows[].peer-status",
        "metrics": [
          "opnsense_wireguard_peer_status"
        ],
        "exercise": "The VALUE of the peer_status gauge, on type=\"peer\" rows only - the wgtest1/wgtest2 loopback pair with its short persistent-keepalive gives the online branch within seconds. Like the interface status it is a TRANSLATED string: parseWGPeerStatus matches \"online\"/\"offline\"/\"stale\" and reports 2 (unknown) with a log warning for anything else, so drift degrades to a permanent 2 rather than a failed scrape. The stale branch needs a peer idle for more than 300s, and offline needs the peer instance stopped.",
        "verified": "LIVE-VERIFIED 2026-07-31 on BOTH testbeds, replacing a blocker that named the wrong box. The empty rows array that blocker was written from came from the PROD canary profile (#531), not from a testbed: camden's prod canary stamps its report `--generation \"release <version>\"` (opnsense-prod-canary.sh), and \"release\" there means the production firewall's release CHANNEL, not the release-channel testbed VM - reading it as the testbed is how prod box state got filed as a testbed regression. Direct check on both testbed VMs: `configctl wireguard show` returns the wgtest loopback pair with type=\"interface\" rows up and type=\"peer\" rows carrying a fresh handshake and non-zero transfer counters in both directions; name/ifname/peer-status are added on top of those records by the API controller (/usr/local/opnsense/mvc/app/controllers/OPNsense/Wireguard/Api/ServiceController.php, lines 90-117) and are therefore present on every row the script emitted. The same day's nightly and release-vm canary reports (#608 / #607) carry NO required-coverage section at all for any endpoint, which is the canary's own confirmation that all nine of this endpoint's paths resolve on both boxes."
      }
    ],
    "note": "WireGuard is enabled on BOTH testbeds - re-verified live 2026-07-31 - and must stay that way, or the release box silently loses these paths again. CORRECTION 2026-07-31: the previous wording of this note ended \"...while still answering the endpoint with its interface rows\", and the rows[].latest-handshake recipe carried the same claim. Both were WRONG; see that recipe for the upstream-source evidence. THE PROD PROFILE CAN NEVER CLEAR THESE PATHS AND IT IS NOT A TESTBED-PROVISIONING TASK: the production firewall has zero WireGuard instances configured (/usr/local/etc/wireguard is empty, `configctl wireguard show` returns records: []), and on 26.7 WireGuard is in-base rather than a plugin, so the endpoint answers 200 with an empty rows array instead of the 404 that would classify it plugin-gated. That is why the prod canary issue (#531) reports all nine of these paths as required-coverage warnings while both testbed canary issues (#607 / #608) report none. Provisioning a testbed cannot change it, and no exemption can suppress it either - missingOK only suppresses ValidationResult.Missing, while an empty parent array files its children under ValidationResult.Unverified (opnsense/schema_validate.go:545-549), which is what the coverage ledger reads. Closing that gap needs per-profile scoping on the coverage ledger, mirroring SchemaExemption.Profiles - a code change, not a ledger edit. AUDIT ADDENDUM 2026-07-31 (#599): #599's counter-check that rows[].peer-status has no collector consumer is WRONG and is corrected here. opnsense/wireguard.go:133 passes it through parseWGPeerStatus into WireguardPeers.Status, which internal/collector/wireguard.go:126 emits as the VALUE of opnsense_wireguard_peer_status - it is not a spare presentation field, it is the entire peer health signal. All six of this endpoint's remaining unclassified paths back a metric; rows[] itself is the element container and is left informational."
  },
  "captivePortalSessions": {
    "required": [
      {
        "path": "rows[].zoneid",
        "opaque": true,
        "metrics": [
          "opnsense_captiveportal_zone_sessions"
        ],
        "exercise": "Configure one captive-portal zone (Services > Captive Portal > Administration: add a zone, enable it, bind it to the interface the traffgen LXC sits on) and then authenticate one client through the portal, so api/captiveportal/session/search returns a row. BOTH steps are needed and the second is the easy one to skip: a configured portal with nobody logged in still answers {total:0,rows:[]}, and because captiveportal.go emits a zero-valued zone_sessions series for every configured zone, the metric reads a perfectly correct 0 while this path stays unexercised.",
        "blocker": "no captive-portal zone exists on either testbed and no client has ever authenticated through one. This is the field the per-zone attribution turns on: if zoneid is renamed upstream, every session falls into the synthetic \"unknown\" zone, so every configured zone's series silently reads 0 while opnsense_captiveportal_sessions stays correct (it comes from the envelope's own count, not from these rows) - a drift that looks like an idle portal rather than a bug."
      }
    ],
    "note": "AUDITED 2026-07-31 (#599). rows[] itself is the array element container, not a value: it carries no metric and its warning would resolve with the leaf above, so it is left informational - the same treatment carpStatus's rows[] already gets alongside nine ledgered children. Per-session PII (userName/macAddress/ipAddress) is deliberately not modelled at all (D10), so it cannot appear here."
  },
  "captivePortalVoucherProviders": {
    "required": [
      {
        "path": "[]",
        "metrics": [
          "opnsense_captiveportal_vouchers",
          "opnsense_captiveportal_voucher_group_next_expiry_timestamp_seconds"
        ],
        "exercise": "Create one voucher-type authentication server (System > Access > Servers, type \"Voucher\"). api/captiveportal/voucher/list_providers IS the list of those servers, so one is enough to make the array non-empty. Configuration only: no voucher needs to be generated and none needs to be redeemed.",
        "blocker": "no voucher auth server exists on either testbed, so list_providers returns [] and the whole voucher fetch short-circuits before it ever calls list_voucher_groups or list_vouchers - which is also why captivePortalVoucherGroups and captivePortalVouchers sit in the canary's skipped-parameterized list. Note this path is the ELEMENT of a bare string array, so unlike a rows[] container it is the value itself: each element becomes the provider label on both metrics and the path segment of the two downstream calls."
      }
    ],
    "note": "AUDITED 2026-07-31 (#599). One provider unlocks three endpoints: the group and voucher calls are parameterized by this array's elements, so an empty list leaves that whole subtree unprobed rather than merely unlabelled."
  },
  "firmware": {
    "required": [
      {
        "path": "product.product_check.upgrade_needs_reboot",
        "metrics": [
          "opnsense_firmware_upgrade_needs_reboot"
        ],
        "exercise": "Make the box store an update check (System > Firmware > Updates \"Check for updates\", or `configctl firmware check`), then re-poll. FirmwareController.php:126-127 does `$response = $product['product_check']; $response['product'] = $product;` - the top level IS product_check and `product` re-serves the same object - so the nested copy appears at the same moment the top-level superset does. A box that has not checked since boot serves the bare three-key envelope and product.product_check arrives as PHP's empty-array-for-empty-object, which the validator files as unverified rather than missing.",
        "blocker": "the canary probes whatever state the box is in and this run caught it pre-check. Sharper than a plain box-state gap, though: this nested copy is the FALLBACK arm of a tolerant reader - firmwareStatusResponse.upgradeNeedsReboot() prefers the top-level upgrade_needs_reboot pointer and only reads this path when that key is absent. So on a box that sends the top-level key the metric is right while this path goes unchecked, and on a box that does not, the metric is computed entirely from an unverified path. The fallback exists for exactly the latter kind of box, which is why it is required coverage and not decoration."
      }
    ],
    "note": "AUDITED 2026-07-31 (#599). product.product_check (the object itself) is left informational: it is the parent container of the one leaf modelled under it, carries no value of its own, and resolves at the same instant that leaf does. Everything else under product.* is exempted in exemptions.json as duplicate-representation identity metadata, so no other path here is a candidate."
  },
  "idsQueryAlerts": {
    "required": [
      {
        "path": "rows[].timestamp",
        "opaque": true,
        "metrics": [
          "opnsense_ids_recent_alerts"
        ],
        "exercise": "Requires --exporter.enable-ids-alerts AND at least one Suricata alert inside --exporter.ids-alert-lookback at the moment the canary polls. Enable IDS (Services > Intrusion Detection > Administration: enabled=1, at least one ruleset downloaded and enabled, on the interface the traffgen LXC's traffic crosses), then trip a rule from that LXC - one HTTP request matching an ET-open signature is enough. The window is the catch: alerts age out of the lookback, so this path is verifiable only while traffic is being generated, and query_alerts is itself a windowed saturating read (a floor above 500 alerts). parseIDSAlertTime is what turns this into the in-window test, so a format change here silently empties the window rather than failing.",
        "blocker": "api/ids/service/query_alerts returned an empty rows array on the release-channel testbed - either Suricata is not enabled there or nothing has alerted inside the retained eve window. Check `configctl service status suricata` and the eve log inventory before treating a regression here as drift. Both paths are flex types (KindAny), so even a populated run can only ever prove the key exists; the value evidence is ids_test.go's fixtures."
      },
      {
        "path": "rows[].alert_action",
        "opaque": true,
        "metrics": [
          "opnsense_ids_recent_alerts"
        ],
        "exercise": "Requires --exporter.enable-ids-alerts AND at least one Suricata alert inside --exporter.ids-alert-lookback at the moment the canary polls. Enable IDS (Services > Intrusion Detection > Administration: enabled=1, at least one ruleset downloaded and enabled, on the interface the traffgen LXC's traffic crosses), then trip a rule from that LXC - one HTTP request matching an ET-open signature is enough. The window is the catch: alerts age out of the lookback, so this path is verifiable only while traffic is being generated, and query_alerts is itself a windowed saturating read (a floor above 500 alerts). Both label values are pre-seeded to 0 so the series never disappears on a quiet scrape, and only \"allowed\"/\"blocked\" are counted - an unrecognised action is dropped, not bucketed as unknown, so a renamed value reads as zero alerts rather than as drift. Exercising the blocked branch needs IPS mode on and a drop rule.",
        "blocker": "api/ids/service/query_alerts returned an empty rows array on the release-channel testbed - either Suricata is not enabled there or nothing has alerted inside the retained eve window. Check `configctl service status suricata` and the eve log inventory before treating a regression here as drift. Both paths are flex types (KindAny), so even a populated run can only ever prove the key exists; the value evidence is ids_test.go's fixtures."
      }
    ],
    "note": "AUDITED 2026-07-31 (#599). Only these two of the nine modelled row fields back a metric. alert, alert_sid, dest_ip, flow_id, in_iface, proto and src_ip are consumed ONLY by FetchIDSAlertRecords, which feeds the logs.ids LOG source (cursor dedupe plus bounded structured-metadata attributes) - FetchIDSRecentAlerts, the metrics path, ignores them outright, as idsAlertRow's own field comment says. They are deliberately left informational: this ledger's required class means \"backs an emitted METRIC\", and stretching it to cover log attributes would be a scope change to the ledger's contract, not an audit finding. #599's counter-check that rows[].alert_sid has no collector consumer is CONFIRMED."
  },
  "ipsecPhase1": {
    "required": [
      {
        "path": "rows[].phase1desc",
        "metrics": [
          "opnsense_ipsec_phase1_status",
          "opnsense_ipsec_phase1_install_time",
          "opnsense_ipsec_phase1_bytes_in_total",
          "opnsense_ipsec_phase1_bytes_out_total",
          "opnsense_ipsec_phase1_packets_in_total",
          "opnsense_ipsec_phase1_packets_out_total"
        ],
        "exercise": "One site-to-site IPsec tunnel between the two testbed OPNsense boxes across the isolated bridge (VPN > IPsec, matching PSK and proposals on both, one phase2 traffic selector each side), then traffic through it so the byte/packet counters are non-zero. api/ipsec/sessions/search_phase1 reports LIVE IKE SAs, not configured connections - every field on the row is live-SA state (connected, install-time, cumulative bytes) - so a tunnel that is configured but has never been initiated leaves rows[] empty and looks identical to no config at all. Initiate it from one side and confirm with `swanctl --list-sas` on the box before treating an empty array as drift. This is the description label on every phase1 series.",
        "blocker": "no IPsec tunnel is up on the release-channel testbed: search_phase1 returned an empty rows array, so nothing under rows[] has ever been checked there. ipsecPhase2 cannot be probed at all without a live ikeid (it is in the canary's skipped-parameterized list and is covered by the e2e smoke instead), so phase1 rows are also the only route to an ikeid. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].name",
        "metrics": [
          "opnsense_ipsec_phase1_status",
          "opnsense_ipsec_phase1_install_time",
          "opnsense_ipsec_phase1_bytes_in_total",
          "opnsense_ipsec_phase1_bytes_out_total",
          "opnsense_ipsec_phase1_packets_in_total",
          "opnsense_ipsec_phase1_packets_out_total",
          "opnsense_ipsec_phase2_established",
          "opnsense_ipsec_phase2_install_time",
          "opnsense_ipsec_phase2_bytes_in_total",
          "opnsense_ipsec_phase2_bytes_out_total",
          "opnsense_ipsec_phase2_packets_in_total",
          "opnsense_ipsec_phase2_packets_out_total",
          "opnsense_ipsec_phase2_rekey_time",
          "opnsense_ipsec_phase2_life_time"
        ],
        "exercise": "One site-to-site IPsec tunnel between the two testbed OPNsense boxes across the isolated bridge (VPN > IPsec, matching PSK and proposals on both, one phase2 traffic selector each side), then traffic through it so the byte/packet counters are non-zero. api/ipsec/sessions/search_phase1 reports LIVE IKE SAs, not configured connections - every field on the row is live-SA state (connected, install-time, cumulative bytes) - so a tunnel that is configured but has never been initiated leaves rows[] empty and looks identical to no config at all. Initiate it from one side and confirm with `swanctl --list-sas` on the box before treating an empty array as drift. It is the name label on every phase1 series AND the phase1_name label that joins every phase2 series back to its parent tunnel.",
        "blocker": "no IPsec tunnel is up on the release-channel testbed: search_phase1 returned an empty rows array, so nothing under rows[] has ever been checked there. ipsecPhase2 cannot be probed at all without a live ikeid (it is in the canary's skipped-parameterized list and is covered by the e2e smoke instead), so phase1 rows are also the only route to an ikeid. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].connected",
        "metrics": [
          "opnsense_ipsec_phase1_status"
        ],
        "exercise": "One site-to-site IPsec tunnel between the two testbed OPNsense boxes across the isolated bridge (VPN > IPsec, matching PSK and proposals on both, one phase2 traffic selector each side), then traffic through it so the byte/packet counters are non-zero. api/ipsec/sessions/search_phase1 reports LIVE IKE SAs, not configured connections - every field on the row is live-SA state (connected, install-time, cumulative bytes) - so a tunnel that is configured but has never been initiated leaves rows[] empty and looks identical to no config at all. Initiate it from one side and confirm with `swanctl --list-sas` on the box before treating an empty array as drift. Both branches need the tunnel to go down as well as up: drop the bridge link or stop the peer VM for the 0 branch. Note this is an IKE-SA-level aggregate - it says nothing about any individual child SA, which is what phase2_established exists for.",
        "blocker": "no IPsec tunnel is up on the release-channel testbed: search_phase1 returned an empty rows array, so nothing under rows[] has ever been checked there. ipsecPhase2 cannot be probed at all without a live ikeid (it is in the canary's skipped-parameterized list and is covered by the e2e smoke instead), so phase1 rows are also the only route to an ikeid. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].install-time",
        "metrics": [
          "opnsense_ipsec_phase1_install_time"
        ],
        "exercise": "One site-to-site IPsec tunnel between the two testbed OPNsense boxes across the isolated bridge (VPN > IPsec, matching PSK and proposals on both, one phase2 traffic selector each side), then traffic through it so the byte/packet counters are non-zero. api/ipsec/sessions/search_phase1 reports LIVE IKE SAs, not configured connections - every field on the row is live-SA state (connected, install-time, cumulative bytes) - so a tunnel that is configured but has never been initiated leaves rows[] empty and looks identical to no config at all. Initiate it from one side and confirm with `swanctl --list-sas` on the box before treating an empty array as drift. Served as a numeric STRING and parsed with strconv.Atoi, falling back to 0 on any parse failure - so a representation change reads as a freshly installed SA rather than as an error.",
        "blocker": "no IPsec tunnel is up on the release-channel testbed: search_phase1 returned an empty rows array, so nothing under rows[] has ever been checked there. ipsecPhase2 cannot be probed at all without a live ikeid (it is in the canary's skipped-parameterized list and is covered by the e2e smoke instead), so phase1 rows are also the only route to an ikeid. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].bytes-in",
        "metrics": [
          "opnsense_ipsec_phase1_bytes_in_total"
        ],
        "exercise": "One site-to-site IPsec tunnel between the two testbed OPNsense boxes across the isolated bridge (VPN > IPsec, matching PSK and proposals on both, one phase2 traffic selector each side), then traffic through it so the byte/packet counters are non-zero. api/ipsec/sessions/search_phase1 reports LIVE IKE SAs, not configured connections - every field on the row is live-SA state (connected, install-time, cumulative bytes) - so a tunnel that is configured but has never been initiated leaves rows[] empty and looks identical to no config at all. Initiate it from one side and confirm with `swanctl --list-sas` on the box before treating an empty array as drift. Real JSON numbers here (unlike phase2's string counters), decoded as int64 so a counter above 2^31 survives on 32-bit builds (#103).",
        "blocker": "no IPsec tunnel is up on the release-channel testbed: search_phase1 returned an empty rows array, so nothing under rows[] has ever been checked there. ipsecPhase2 cannot be probed at all without a live ikeid (it is in the canary's skipped-parameterized list and is covered by the e2e smoke instead), so phase1 rows are also the only route to an ikeid. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].bytes-out",
        "metrics": [
          "opnsense_ipsec_phase1_bytes_out_total"
        ],
        "exercise": "One site-to-site IPsec tunnel between the two testbed OPNsense boxes across the isolated bridge (VPN > IPsec, matching PSK and proposals on both, one phase2 traffic selector each side), then traffic through it so the byte/packet counters are non-zero. api/ipsec/sessions/search_phase1 reports LIVE IKE SAs, not configured connections - every field on the row is live-SA state (connected, install-time, cumulative bytes) - so a tunnel that is configured but has never been initiated leaves rows[] empty and looks identical to no config at all. Initiate it from one side and confirm with `swanctl --list-sas` on the box before treating an empty array as drift. Same as rows[].bytes-in.",
        "blocker": "no IPsec tunnel is up on the release-channel testbed: search_phase1 returned an empty rows array, so nothing under rows[] has ever been checked there. ipsecPhase2 cannot be probed at all without a live ikeid (it is in the canary's skipped-parameterized list and is covered by the e2e smoke instead), so phase1 rows are also the only route to an ikeid. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].packets-in",
        "metrics": [
          "opnsense_ipsec_phase1_packets_in_total"
        ],
        "exercise": "One site-to-site IPsec tunnel between the two testbed OPNsense boxes across the isolated bridge (VPN > IPsec, matching PSK and proposals on both, one phase2 traffic selector each side), then traffic through it so the byte/packet counters are non-zero. api/ipsec/sessions/search_phase1 reports LIVE IKE SAs, not configured connections - every field on the row is live-SA state (connected, install-time, cumulative bytes) - so a tunnel that is configured but has never been initiated leaves rows[] empty and looks identical to no config at all. Initiate it from one side and confirm with `swanctl --list-sas` on the box before treating an empty array as drift. Same as rows[].bytes-in.",
        "blocker": "no IPsec tunnel is up on the release-channel testbed: search_phase1 returned an empty rows array, so nothing under rows[] has ever been checked there. ipsecPhase2 cannot be probed at all without a live ikeid (it is in the canary's skipped-parameterized list and is covered by the e2e smoke instead), so phase1 rows are also the only route to an ikeid. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].packets-out",
        "metrics": [
          "opnsense_ipsec_phase1_packets_out_total"
        ],
        "exercise": "One site-to-site IPsec tunnel between the two testbed OPNsense boxes across the isolated bridge (VPN > IPsec, matching PSK and proposals on both, one phase2 traffic selector each side), then traffic through it so the byte/packet counters are non-zero. api/ipsec/sessions/search_phase1 reports LIVE IKE SAs, not configured connections - every field on the row is live-SA state (connected, install-time, cumulative bytes) - so a tunnel that is configured but has never been initiated leaves rows[] empty and looks identical to no config at all. Initiate it from one side and confirm with `swanctl --list-sas` on the box before treating an empty array as drift. Same as rows[].bytes-in.",
        "blocker": "no IPsec tunnel is up on the release-channel testbed: search_phase1 returned an empty rows array, so nothing under rows[] has ever been checked there. ipsecPhase2 cannot be probed at all without a live ikeid (it is in the canary's skipped-parameterized list and is covered by the e2e smoke instead), so phase1 rows are also the only route to an ikeid. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].ikeid",
        "metrics": [
          "opnsense_ipsec_phase2_established",
          "opnsense_ipsec_phase2_install_time",
          "opnsense_ipsec_phase2_bytes_in_total",
          "opnsense_ipsec_phase2_bytes_out_total",
          "opnsense_ipsec_phase2_packets_in_total",
          "opnsense_ipsec_phase2_packets_out_total",
          "opnsense_ipsec_phase2_rekey_time",
          "opnsense_ipsec_phase2_life_time"
        ],
        "exercise": "One site-to-site IPsec tunnel between the two testbed OPNsense boxes across the isolated bridge (VPN > IPsec, matching PSK and proposals on both, one phase2 traffic selector each side), then traffic through it so the byte/packet counters are non-zero. api/ipsec/sessions/search_phase1 reports LIVE IKE SAs, not configured connections - every field on the row is live-SA state (connected, install-time, cumulative bytes) - so a tunnel that is configured but has never been initiated leaves rows[] empty and looks identical to no config at all. Initiate it from one side and confirm with `swanctl --list-sas` on the box before treating an empty array as drift. ikeid is NOT a label on any series - it is the id FetchIPsecPhase1 POSTs to api/ipsec/sessions/searchPhase2 once per phase1 row, so it is the only route to phase2 data at all. If it is renamed, the POST goes out with an empty id and every opnsense_ipsec_phase2_* series vanishes while phase1 keeps reporting a healthy tunnel; the fetch logs an error and continues by design (partial-failure tolerance), so nothing about the scrape looks wrong.",
        "blocker": "no IPsec tunnel is up on the release-channel testbed: search_phase1 returned an empty rows array, so nothing under rows[] has ever been checked there. ipsecPhase2 cannot be probed at all without a live ikeid (it is in the canary's skipped-parameterized list and is covered by the e2e smoke instead), so phase1 rows are also the only route to an ikeid. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      }
    ],
    "note": "AUDITED 2026-07-31 (#599). All nine modelled row fields back a metric; rows[] itself is the element container and is left informational. Note phase1's byte/packet counters are the aggregate of the child SAs, so they are typed as Counters to match the phase2 counters they sum (#106)."
  },
  "ipsecSad": {
    "required": [
      {
        "path": "rows[].satype",
        "metrics": [
          "opnsense_ipsec_sad_entries"
        ],
        "exercise": "Same tunnel as ipsecPhase1: the kernel SAD (setkey -D, via api/ipsec/sad/search) populates the moment a child SA installs, so no extra step beyond a tunnel that has actually come up. satype is both the grouping label on sad_entries and half of the placeholder-row test: FetchIPsecSAD discards any row whose spi or satype is empty, which is how the synthetic \"No SAD entries.\" row (both null) is dropped. A rename therefore does not mislabel the series, it deletes every row.",
        "blocker": "no IPsec tunnel is up on the release-channel testbed: search_phase1 returned an empty rows array, so nothing under rows[] has ever been checked there. ipsecPhase2 cannot be probed at all without a live ikeid (it is in the canary's skipped-parameterized list and is covered by the e2e smoke instead), so phase1 rows are also the only route to an ikeid. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].spi",
        "metrics": [
          "opnsense_ipsec_sad_entries",
          "opnsense_ipsec_sa_age_seconds",
          "opnsense_ipsec_sa_lifetime_hard_seconds",
          "opnsense_ipsec_sa_lifetime_soft_seconds",
          "opnsense_ipsec_sa_bytes_current_total",
          "opnsense_ipsec_sa_bytes_soft_limit",
          "opnsense_ipsec_sa_bytes_hard_limit",
          "opnsense_ipsec_sa_allocated_current_total",
          "opnsense_ipsec_sa_allocated_soft_limit",
          "opnsense_ipsec_sa_allocated_hard_limit",
          "opnsense_ipsec_sad_nat_traversal"
        ],
        "exercise": "Same tunnel as ipsecPhase1: the kernel SAD (setkey -D, via api/ipsec/sad/search) populates the moment a child SA installs, so no extra step beyond a tunnel that has actually come up. spi is deliberately never a label (it churns on every rekey, so it would explode cardinality). It is ledgered because it is the other half of the placeholder-row discriminator: if upstream renames it, every row reads as the synthetic placeholder and is discarded, and the entire kernel-SA metric family silently disappears from a firewall with healthy tunnels. That is the worst failure shape this ledger exists to catch, and it has no label or value to notice it by.",
        "blocker": "no IPsec tunnel is up on the release-channel testbed: search_phase1 returned an empty rows array, so nothing under rows[] has ever been checked there. ipsecPhase2 cannot be probed at all without a live ikeid (it is in the canary's skipped-parameterized list and is covered by the e2e smoke instead), so phase1 rows are also the only route to an ikeid. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].ikeid",
        "opaque": true,
        "metrics": [
          "opnsense_ipsec_sad_entries",
          "opnsense_ipsec_sa_age_seconds",
          "opnsense_ipsec_sa_lifetime_hard_seconds",
          "opnsense_ipsec_sa_lifetime_soft_seconds",
          "opnsense_ipsec_sa_bytes_current_total",
          "opnsense_ipsec_sa_bytes_soft_limit",
          "opnsense_ipsec_sa_bytes_hard_limit",
          "opnsense_ipsec_sa_allocated_current_total",
          "opnsense_ipsec_sa_allocated_soft_limit",
          "opnsense_ipsec_sa_allocated_hard_limit",
          "opnsense_ipsec_sad_nat_traversal"
        ],
        "exercise": "Same tunnel as ipsecPhase1: the kernel SAD (setkey -D, via api/ipsec/sad/search) populates the moment a child SA installs, so no extra step beyond a tunnel that has actually come up. The ikeid label on all three families, and the grouping key for sad_nat_traversal. It comes back null on the live box more often than not, which is precisely why reqid (bounded and rekey-stable) is the series anchor and this is context.",
        "blocker": "no IPsec tunnel is up on the release-channel testbed: search_phase1 returned an empty rows array, so nothing under rows[] has ever been checked there. ipsecPhase2 cannot be probed at all without a live ikeid (it is in the canary's skipped-parameterized list and is covered by the e2e smoke instead), so phase1 rows are also the only route to an ikeid. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field. Flex type (KindAny), so a populated run can only ever prove the key exists."
      },
      {
        "path": "rows[].phase1desc",
        "opaque": true,
        "metrics": [
          "opnsense_ipsec_sad_entries"
        ],
        "exercise": "Same tunnel as ipsecPhase1: the kernel SAD (setkey -D, via api/ipsec/sad/search) populates the moment a child SA installs, so no extra step beyond a tunnel that has actually come up. The phase1desc label on sad_entries; like ikeid it is frequently null on the wire and adds human context rather than identity.",
        "blocker": "no IPsec tunnel is up on the release-channel testbed: search_phase1 returned an empty rows array, so nothing under rows[] has ever been checked there. ipsecPhase2 cannot be probed at all without a live ikeid (it is in the canary's skipped-parameterized list and is covered by the e2e smoke instead), so phase1 rows are also the only route to an ikeid. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field. Flex type (KindAny), so a populated run can only ever prove the key exists."
      },
      {
        "path": "rows[].phase2desc",
        "opaque": true,
        "metrics": [
          "opnsense_ipsec_sa_age_seconds",
          "opnsense_ipsec_sa_lifetime_hard_seconds",
          "opnsense_ipsec_sa_lifetime_soft_seconds",
          "opnsense_ipsec_sa_bytes_current_total",
          "opnsense_ipsec_sa_bytes_soft_limit",
          "opnsense_ipsec_sa_bytes_hard_limit",
          "opnsense_ipsec_sa_allocated_current_total",
          "opnsense_ipsec_sa_allocated_soft_limit",
          "opnsense_ipsec_sa_allocated_hard_limit"
        ],
        "exercise": "Same tunnel as ipsecPhase1: the kernel SAD (setkey -D, via api/ipsec/sad/search) populates the moment a child SA installs, so no extra step beyond a tunnel that has actually come up. The phase2desc label on every per-reqid SA series.",
        "blocker": "no IPsec tunnel is up on the release-channel testbed: search_phase1 returned an empty rows array, so nothing under rows[] has ever been checked there. ipsecPhase2 cannot be probed at all without a live ikeid (it is in the canary's skipped-parameterized list and is covered by the e2e smoke instead), so phase1 rows are also the only route to an ikeid. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field. Flex type (KindAny), so a populated run can only ever prove the key exists."
      }
    ],
    "note": "AUDITED 2026-07-31 (#599). src/dst/state are decoded and never read (cmd/fieldaudit lists all three as exempt dead fields), so they are correctly informational; reqid, addtime_diff, addtime_hard/soft, nat and the bytes/allocated fields were already verified live in this run and never entered the unclassified bucket."
  },
  "ipsecSpd": {
    "required": [
      {
        "path": "rows[].dir",
        "metrics": [
          "opnsense_ipsec_spd_policies"
        ],
        "exercise": "Same tunnel as ipsecPhase1: api/ipsec/spd/search (setkey -DP) lists one policy per direction as soon as a child SA installs, so an established tunnel exercises the in/out pair without further work; fwd needs a transport-mode or forwarded selector.",
        "blocker": "no IPsec tunnel is up on the release-channel testbed: search_phase1 returned an empty rows array, so nothing under rows[] has ever been checked there. ipsecPhase2 cannot be probed at all without a live ikeid (it is in the canary's skipped-parameterized list and is covered by the e2e smoke instead), so phase1 rows are also the only route to an ikeid. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field. dir is the ONLY field of this row modelled at all - reqid, spid, seq and pid are deliberately not decoded (they would churn as labels) - so an empty rows array leaves the endpoint's entire consumed surface unchecked. It is also both the label and, by count, the value of spd_policies, and a row with an empty dir is skipped, so a rename empties the metric family rather than mislabelling it."
      }
    ],
    "note": "AUDITED 2026-07-31 (#599). rows[] itself is the element container and is left informational."
  },
  "keaSubnets4": {
    "required": [
      {
        "path": "rows[].subnet",
        "metrics": [
          "opnsense_kea_dhcp4_pool_size",
          "opnsense_kea_dhcp4_pool_used"
        ],
        "exercise": "Configure one Kea DHCPv4 subnet with a pool range on the testbed VLAN (Services > Kea DHCP > DHCPv4 > Subnets) so api/kea/dhcpv4/searchSubnet returns a row. Configuration only - no client needs to take a lease, though the same subnet is what the traffgen LXC's lease comes from, so this resolves alongside keaLeases4. It is the subnet label on pool_size and BOTH the label and the grouping key of pool_used, which KeaPoolUsedBySubnet computes by parsing this value as a CIDR and matching lease addresses into it client-side (Kea lease records carry no subnet id). A representation change that still parses as a CIDR would silently re-bucket utilisation instead of failing.",
        "blocker": "searchSubnet returned an empty rows array on the release-channel testbed: the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].pools",
        "metrics": [
          "opnsense_kea_dhcp4_pool_size"
        ],
        "exercise": "Configure one Kea DHCPv4 subnet with a pool range on the testbed VLAN (Services > Kea DHCP > DHCPv4 > Subnets) so api/kea/dhcpv4/searchSubnet returns a row. Configuration only - no client needs to take a lease, though the same subnet is what the traffgen LXC's lease comes from, so this resolves alongside keaLeases4. pools is the VALUE of pool_size: poolSpecSize parses the configured pool SPEC (range or CIDR form) and sums the address capacity. A subnet configured with no pool at all yields 0, which is a real state, so make sure the testbed subnet actually carries a range or this path proves nothing.",
        "blocker": "searchSubnet returned an empty rows array on the release-channel testbed: the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].%interface",
        "metrics": [
          "opnsense_kea_dhcp4_pool_size"
        ],
        "exercise": "Configure one Kea DHCPv4 subnet with a pool range on the testbed VLAN (Services > Kea DHCP > DHCPv4 > Subnets) so api/kea/dhcpv4/searchSubnet returns a row. Configuration only - no client needs to take a lease, though the same subnet is what the traffgen LXC's lease comes from, so this resolves alongside keaLeases4. The interface label on pool_size, and the reason it exists: it carries the same display name as the lease rows' if_descr, which is what makes pool_size joinable against opnsense_kea_dhcp4_leases_by_interface in PromQL. A drift here breaks the join, not the series.",
        "blocker": "searchSubnet returned an empty rows array on the release-channel testbed: the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      }
    ],
    "note": "AUDITED 2026-07-31 (#599). rows[].uuid is deliberately NOT ledgered: KeaSubnet.UUID is read at exactly one place, building the subnet6ByUUID map for the DHCPv6 PD-pool join, and that map is built from FetchKeaSubnets6 only - the v4 subnets never enter it, so no metric depends on the v4 uuid. keaSubnets6's uuid is a different matter and is covered by keaPdPools6's join. rows[] itself is the element container and is left informational."
  },
  "natOneToOneRules": {
    "required": [
      {
        "path": "rows[].enabled",
        "opaque": true,
        "metrics": [
          "opnsense_firewall_nat_rules"
        ],
        "exercise": "Add one 1:1 NAT (BINAT) rule through the MVC UI (Firewall > NAT > One-to-One) and apply. The search_rule endpoints only see MVC-managed rows, so a rule that predates the MVC NAT backend is invisible here no matter how it is configured. Requires --exporter.enable-firewall-nat-counts (four extra GETs per poll). Config only, no traffic. To exercise both branches of isEnabled() the testbed needs one enabled AND one disabled rule of this type: only the literal string \"1\" counts as enabled, and every other value - including an absent key - falls into the disabled bucket, so a rename reports every rule as disabled rather than failing.",
        "blocker": "no 1:1 NAT (BINAT) rule is configured on either testbed, so search_rule returns an empty rows array and both label values of opnsense_firewall_nat_rules report a legitimate 0 for this type. flexString (KindAny), so even a populated run can only ever prove the key exists. Note the sibling d_nat endpoint inverts the encoding into a \"disabled\" field and has its own row struct, so nothing here covers that one."
      }
    ],
    "note": "AUDITED 2026-07-31 (#599). enabled is the ONLY field of this row modelled at all, so an empty rows array leaves the endpoint's entire consumed surface unchecked. rows[] itself is the element container and is left informational."
  },
  "natNPTRules": {
    "required": [
      {
        "path": "rows[].enabled",
        "opaque": true,
        "metrics": [
          "opnsense_firewall_nat_rules"
        ],
        "exercise": "Add one NPTv6 rule through the MVC UI (Firewall > NAT > NPTv6) and apply. The search_rule endpoints only see MVC-managed rows, so a rule that predates the MVC NAT backend is invisible here no matter how it is configured. Requires --exporter.enable-firewall-nat-counts (four extra GETs per poll). Config only, no traffic. To exercise both branches of isEnabled() the testbed needs one enabled AND one disabled rule of this type: only the literal string \"1\" counts as enabled, and every other value - including an absent key - falls into the disabled bucket, so a rename reports every rule as disabled rather than failing.",
        "blocker": "no NPTv6 rule is configured on either testbed, so search_rule returns an empty rows array and both label values of opnsense_firewall_nat_rules report a legitimate 0 for this type. flexString (KindAny), so even a populated run can only ever prove the key exists. Note the sibling d_nat endpoint inverts the encoding into a \"disabled\" field and has its own row struct, so nothing here covers that one."
      }
    ],
    "note": "AUDITED 2026-07-31 (#599). Same shape as natOneToOneRules - one modelled field, sharing the natRuleRow struct and the same enabled encoding. rows[] itself is the element container and is left informational."
  },
  "openVPNInstances": {
    "required": [
      {
        "path": "rows[].enabled",
        "metrics": [
          "opnsense_openvpn_instances"
        ],
        "exercise": "Configure one OpenVPN instance (VPN > OpenVPN > Instances: role=server, dev_type=tun, enabled=1) on a testbed box. api/openvpn/instances/search is pure config, so the instance does not need to start and nothing needs to connect - which makes this the cheapest of the two OpenVPN endpoints to cover and a prerequisite for the other. enabled is the VALUE of the instances gauge. It is parsed with parseStringToInt and a parse failure aborts the WHOLE fetch with an API error rather than defaulting - so unlike most fields here, drift on this one is loud.",
        "blocker": "no OpenVPN instance is configured on the release-channel testbed: instances/search returned an empty rows array. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].uuid",
        "metrics": [
          "opnsense_openvpn_instances",
          "opnsense_openvpn_instance_max_clients"
        ],
        "exercise": "Configure one OpenVPN instance (VPN > OpenVPN > Instances: role=server, dev_type=tun, enabled=1) on a testbed box. api/openvpn/instances/search is pure config, so the instance does not need to start and nothing needs to connect - which makes this the cheapest of the two OpenVPN endpoints to cover and a prerequisite for the other. The uuid label on both series.",
        "blocker": "no OpenVPN instance is configured on the release-channel testbed: instances/search returned an empty rows array. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].description",
        "metrics": [
          "opnsense_openvpn_instances",
          "opnsense_openvpn_instance_max_clients"
        ],
        "exercise": "Configure one OpenVPN instance (VPN > OpenVPN > Instances: role=server, dev_type=tun, enabled=1) on a testbed box. api/openvpn/instances/search is pure config, so the instance does not need to start and nothing needs to connect - which makes this the cheapest of the two OpenVPN endpoints to cover and a prerequisite for the other. The description label on both series. It is also the join key an operator uses to divide sessions_by_instance by instance_max_clients for a utilisation ratio, but note that label comes from the SESSIONS endpoint's own description field, not from this one - the join is across two endpoints agreeing on a string.",
        "blocker": "no OpenVPN instance is configured on the release-channel testbed: instances/search returned an empty rows array. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].role",
        "metrics": [
          "opnsense_openvpn_instances",
          "opnsense_openvpn_instance_max_clients"
        ],
        "exercise": "Configure one OpenVPN instance (VPN > OpenVPN > Instances: role=server, dev_type=tun, enabled=1) on a testbed box. api/openvpn/instances/search is pure config, so the instance does not need to start and nothing needs to connect - which makes this the cheapest of the two OpenVPN endpoints to cover and a prerequisite for the other. The role label on both series, lowercased by the client. It also explains max_clients' presence gating: a client-role instance has no maxclients concept, so it emits no instance_max_clients series at all rather than a fabricated 0.",
        "blocker": "no OpenVPN instance is configured on the release-channel testbed: instances/search returned an empty rows array. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].dev_type",
        "metrics": [
          "opnsense_openvpn_instances",
          "opnsense_openvpn_instance_max_clients"
        ],
        "exercise": "Configure one OpenVPN instance (VPN > OpenVPN > Instances: role=server, dev_type=tun, enabled=1) on a testbed box. api/openvpn/instances/search is pure config, so the instance does not need to start and nothing needs to connect - which makes this the cheapest of the two OpenVPN endpoints to cover and a prerequisite for the other. The device_type label on both series; tun and tap are both worth having on the testbed only if the label ever drives a decision - one is enough to verify the path.",
        "blocker": "no OpenVPN instance is configured on the release-channel testbed: instances/search returned an empty rows array. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      }
    ],
    "note": "AUDITED 2026-07-31 (#599). All five modelled row fields back a metric. maxclients does too and is not listed here because this run verified it live rather than leaving it unverified. rows[] itself is the element container and is left informational."
  },
  "openVPNSessions": {
    "required": [
      {
        "path": "rows[].is_client",
        "opaque": true,
        "metrics": [
          "opnsense_openvpn_current_sessions",
          "opnsense_openvpn_sessions_by_instance",
          "opnsense_openvpn_instance_received_bytes_total",
          "opnsense_openvpn_instance_transmitted_bytes_total",
          "opnsense_openvpn_sessions",
          "opnsense_openvpn_session_received_bytes_total",
          "opnsense_openvpn_session_transmitted_bytes_total",
          "opnsense_openvpn_session_connected_since_timestamp_seconds"
        ],
        "exercise": "Needs a real client SESSION, not just a configured instance: stand the server instance up (see openVPNInstances) and connect a client to it, then push traffic so the byte counters move. The cheapest arrangement is the wgtest1/wgtest2 model from #460 - a second instance in client role on the same box, pointed at the server over the loopback - which needs no external peer and no second VM. Alternatively run an openvpn client on the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). is_client is the row-inclusion gate: FetchOpenVPNSessions drops every row without it, which is how the synthetic idle-server and stopped-server rows are kept out of the session count (#88). It is therefore not a label or a value anywhere - it decides which rows become sessions - and because it defaults to false when absent, a rename makes EVERY session vanish and sessions_total read 0 on a server full of clients.",
        "blocker": "api/openvpn/service/search_sessions returned an empty rows array on the release-channel testbed, which means no instance exists at all: searchSessionsAction emits a synthetic row for a running server with zero clients and a stub row per enabled-but-stopped server, so a box with any configured instance answers with rows even when nobody is connected. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field. flexBool (KindAny), so even a populated run can only ever prove the key exists."
      },
      {
        "path": "rows[].description",
        "metrics": [
          "opnsense_openvpn_current_sessions",
          "opnsense_openvpn_sessions_by_instance",
          "opnsense_openvpn_instance_received_bytes_total",
          "opnsense_openvpn_instance_transmitted_bytes_total",
          "opnsense_openvpn_sessions",
          "opnsense_openvpn_session_received_bytes_total",
          "opnsense_openvpn_session_transmitted_bytes_total",
          "opnsense_openvpn_session_connected_since_timestamp_seconds"
        ],
        "exercise": "Needs a real client SESSION, not just a configured instance: stand the server instance up (see openVPNInstances) and connect a client to it, then push traffic so the byte counters move. The cheapest arrangement is the wgtest1/wgtest2 model from #460 - a second instance in client role on the same box, pointed at the server over the loopback - which needs no external peer and no second VM. Alternatively run an openvpn client on the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). The description label on the always-on per-instance aggregates AND the grouping key they are summed by, so a drift here collapses every instance into one series rather than emptying them.",
        "blocker": "api/openvpn/service/search_sessions returned an empty rows array on the release-channel testbed, which means no instance exists at all: searchSessionsAction emits a synthetic row for a running server with zero clients and a stub row per enabled-but-stopped server, so a box with any configured instance answers with rows even when nobody is connected. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].bytes_received",
        "metrics": [
          "opnsense_openvpn_instance_received_bytes_total",
          "opnsense_openvpn_session_received_bytes_total"
        ],
        "exercise": "Needs a real client SESSION, not just a configured instance: stand the server instance up (see openVPNInstances) and connect a client to it, then push traffic so the byte counters move. The cheapest arrangement is the wgtest1/wgtest2 model from #460 - a second instance in client role on the same box, pointed at the server over the loopback - which needs no external peer and no second VM. Alternatively run an openvpn client on the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). Summed per instance into the always-on aggregate and emitted per session behind --exporter.enable-openvpn-details. Decoded as json.Number so both the quoted-numeric-string shape the box actually sends and a bare JSON number work.",
        "blocker": "api/openvpn/service/search_sessions returned an empty rows array on the release-channel testbed, which means no instance exists at all: searchSessionsAction emits a synthetic row for a running server with zero clients and a stub row per enabled-but-stopped server, so a box with any configured instance answers with rows even when nobody is connected. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].bytes_sent",
        "metrics": [
          "opnsense_openvpn_instance_transmitted_bytes_total",
          "opnsense_openvpn_session_transmitted_bytes_total"
        ],
        "exercise": "Needs a real client SESSION, not just a configured instance: stand the server instance up (see openVPNInstances) and connect a client to it, then push traffic so the byte counters move. The cheapest arrangement is the wgtest1/wgtest2 model from #460 - a second instance in client role on the same box, pointed at the server over the loopback - which needs no external peer and no second VM. Alternatively run an openvpn client on the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). Same as rows[].bytes_received.",
        "blocker": "api/openvpn/service/search_sessions returned an empty rows array on the release-channel testbed, which means no instance exists at all: searchSessionsAction emits a synthetic row for a running server with zero clients and a stub row per enabled-but-stopped server, so a box with any configured instance answers with rows even when nobody is connected. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].connected_since__time_t_",
        "metrics": [
          "opnsense_openvpn_session_connected_since_timestamp_seconds"
        ],
        "exercise": "Needs a real client SESSION, not just a configured instance: stand the server instance up (see openVPNInstances) and connect a client to it, then push traffic so the byte counters move. The cheapest arrangement is the wgtest1/wgtest2 model from #460 - a second instance in client role on the same box, pointed at the server over the loopback - which needs no external peer and no second VM. Alternatively run an openvpn client on the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). Requires --exporter.enable-openvpn-details. The double underscore is upstream's PHP field-name mangling, not a typo, and it is unix seconds - an unparseable value yields 0, which reads as 1970 on a dashboard.",
        "blocker": "api/openvpn/service/search_sessions returned an empty rows array on the release-channel testbed, which means no instance exists at all: searchSessionsAction emits a synthetic row for a running server with zero clients and a stub row per enabled-but-stopped server, so a box with any configured instance answers with rows even when nobody is connected. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].status",
        "metrics": [
          "opnsense_openvpn_sessions"
        ],
        "exercise": "Needs a real client SESSION, not just a configured instance: stand the server instance up (see openVPNInstances) and connect a client to it, then push traffic so the byte counters move. The cheapest arrangement is the wgtest1/wgtest2 model from #460 - a second instance in client role on the same box, pointed at the server over the loopback - which needs no external peer and no second VM. Alternatively run an openvpn client on the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). The VALUE of the sessions gauge (1 = ok), via parseOpenVPNsessionStatusToInt, behind --exporter.enable-openvpn-details.",
        "blocker": "api/openvpn/service/search_sessions returned an empty rows array on the release-channel testbed, which means no instance exists at all: searchSessionsAction emits a synthetic row for a running server with zero clients and a stub row per enabled-but-stopped server, so a box with any configured instance answers with rows even when nobody is connected. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].username",
        "metrics": [
          "opnsense_openvpn_sessions"
        ],
        "exercise": "Needs a real client SESSION, not just a configured instance: stand the server instance up (see openVPNInstances) and connect a client to it, then push traffic so the byte counters move. The cheapest arrangement is the wgtest1/wgtest2 model from #460 - a second instance in client role on the same box, pointed at the server over the loopback - which needs no external peer and no second VM. Alternatively run an openvpn client on the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). The username label on the sessions gauge, and the fallback arm of Sessions.Identity() for the other three detail series. A cert-only session sends the literal \"UNDEF\" here, which is why Identity() prefers common_name; both need a client to exist.",
        "blocker": "api/openvpn/service/search_sessions returned an empty rows array on the release-channel testbed, which means no instance exists at all: searchSessionsAction emits a synthetic row for a running server with zero clients and a stub row per enabled-but-stopped server, so a box with any configured instance answers with rows even when nobody is connected. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].common_name",
        "opaque": true,
        "metrics": [
          "opnsense_openvpn_session_received_bytes_total",
          "opnsense_openvpn_session_transmitted_bytes_total",
          "opnsense_openvpn_session_connected_since_timestamp_seconds"
        ],
        "exercise": "Needs a real client SESSION, not just a configured instance: stand the server instance up (see openVPNInstances) and connect a client to it, then push traffic so the byte counters move. The cheapest arrangement is the wgtest1/wgtest2 model from #460 - a second instance in client role on the same box, pointed at the server over the loopback - which needs no external peer and no second VM. Alternatively run an openvpn client on the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). Needs a CERT-authenticated client specifically: a username/password-only session sends null here and Identity() falls back to username, so a password-auth testbed client exercises the fallback and never this path.",
        "blocker": "api/openvpn/service/search_sessions returned an empty rows array on the release-channel testbed, which means no instance exists at all: searchSessionsAction emits a synthetic row for a running server with zero clients and a stub row per enabled-but-stopped server, so a box with any configured instance answers with rows even when nobody is connected. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field. flexString (KindAny), so even a populated run can only ever prove the key exists."
      },
      {
        "path": "rows[].real_address",
        "metrics": [
          "opnsense_openvpn_sessions",
          "opnsense_openvpn_session_received_bytes_total",
          "opnsense_openvpn_session_transmitted_bytes_total",
          "opnsense_openvpn_session_connected_since_timestamp_seconds"
        ],
        "exercise": "Needs a real client SESSION, not just a configured instance: stand the server instance up (see openVPNInstances) and connect a client to it, then push traffic so the byte counters move. The cheapest arrangement is the wgtest1/wgtest2 model from #460 - a second instance in client role on the same box, pointed at the server over the loopback - which needs no external peer and no second VM. Alternatively run an openvpn client on the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). A label on all four detail series (--exporter.enable-openvpn-details).",
        "blocker": "api/openvpn/service/search_sessions returned an empty rows array on the release-channel testbed, which means no instance exists at all: searchSessionsAction emits a synthetic row for a running server with zero clients and a stub row per enabled-but-stopped server, so a box with any configured instance answers with rows even when nobody is connected. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].virtual_address",
        "metrics": [
          "opnsense_openvpn_sessions",
          "opnsense_openvpn_session_received_bytes_total",
          "opnsense_openvpn_session_transmitted_bytes_total",
          "opnsense_openvpn_session_connected_since_timestamp_seconds"
        ],
        "exercise": "Needs a real client SESSION, not just a configured instance: stand the server instance up (see openVPNInstances) and connect a client to it, then push traffic so the byte counters move. The cheapest arrangement is the wgtest1/wgtest2 model from #460 - a second instance in client role on the same box, pointed at the server over the loopback - which needs no external peer and no second VM. Alternatively run an openvpn client on the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). A label on all four detail series (--exporter.enable-openvpn-details).",
        "blocker": "api/openvpn/service/search_sessions returned an empty rows array on the release-channel testbed, which means no instance exists at all: searchSessionsAction emits a synthetic row for a running server with zero clients and a stub row per enabled-but-stopped server, so a box with any configured instance answers with rows even when nobody is connected. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      },
      {
        "path": "rows[].virtual_ipv6_address",
        "metrics": [
          "opnsense_openvpn_sessions",
          "opnsense_openvpn_session_received_bytes_total",
          "opnsense_openvpn_session_transmitted_bytes_total",
          "opnsense_openvpn_session_connected_since_timestamp_seconds"
        ],
        "exercise": "Needs a real client SESSION, not just a configured instance: stand the server instance up (see openVPNInstances) and connect a client to it, then push traffic so the byte counters move. The cheapest arrangement is the wgtest1/wgtest2 model from #460 - a second instance in client role on the same box, pointed at the server over the loopback - which needs no external peer and no second VM. Alternatively run an openvpn client on the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). Empty for a v4-only session, so exercising it needs a dual-stack or v6-only tunnel (#483) - and on this hypervisor that means the client must be a VM, since the host kernel is booted with ipv6.disable=1 and no LXC on it has an IPv6 stack at all (the same root cause recorded on keaLeases6). It is a separate label alongside virtual_address, not a replacement.",
        "blocker": "api/openvpn/service/search_sessions returned an empty rows array on the release-channel testbed, which means no instance exists at all: searchSessionsAction emits a synthetic row for a running server with zero clients and a stub row per enabled-but-stopped server, so a box with any configured instance answers with rows even when nobody is connected. the release-channel testbed is a stripped clone of the devel box, so a feature configured only on the devel box reads as unverified on every release-channel run - the same divergence #508 found for WireGuard. Fix the box, not the ledger: this field backs an exported metric, so an exemption here would blind the canary to real drift on a consumed field."
      }
    ],
    "note": "AUDITED 2026-07-31 (#599). All eleven modelled row fields back a metric - four of them the always-on aggregates, the rest the --exporter.enable-openvpn-details series. rows[] itself is the element container and is left informational. #599's proof case, rows[].bytes_received, is CONFIRMED: it is summed into the per-instance aggregate and emitted per session."
  },
  "trafficShaperStatistics": {
    "required": [
      {
        "path": "items[].type",
        "opaque": true,
        "metrics": [
          "opnsense_trafficshaper_pipes",
          "opnsense_trafficshaper_queues"
        ],
        "exercise": "Configure the shaper and then put traffic through it: one pipe with a bandwidth limit, one queue on that pipe, and one shaper RULE attaching them to an interface (Firewall > Shaper > Pipes / Queues / Rules), apply, then generate traffic across that interface from the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). api/trafficshaper/service/statistics answers {status:ok,items:[]} when the shaper is unconfigured, which is the current state of both testbeds. type is the pipe/queue discriminator: it decides which entity list a row joins, so it directly values both totals AND gates the existence of every opnsense_trafficshaper_pipe_* and queue_* series. An item whose type is neither is skipped outright, so a rename empties the whole subsystem rather than mislabelling it.",
        "blocker": "CORRECTED 2026-08-01 (#621): the previous text here claimed the shaper is not configured on either testbed and that items[] is empty. Both were FALSE and cost a hunt for a configuration that was already there - pipe 10000 @ 200 Mbit/s and rule 'shape TESTLAN' on opt1, which had passed 1.13M packets. items[] has never been empty. What was empty is items[].flows[], because dnctl reports LIVE flows and this pipe has 0 ms delay, so a packet is dequeued almost on arrival and the row lapses between packets; traffgen.sh runs once a MINUTE from cron, so the canary usually polled between bursts. Held open now by shaper-keepalive@.service on the traffgen LXC (a continuous 100 pps / 500-byte ping to each firewall's TESTLAN address - one host covers both, since TESTLAN is a shared segment), measured 12/12 populated where a 2-second interval was 3/5. flex type (KindAny), so even a populated run can only ever prove the key exists; the value evidence is trafficshaper_test.go's fixtures, which are derived from ServiceController.php::statisticsAction and scripts/shaper/lib/__init__.py rather than from a capture."
      },
      {
        "path": "items[].id",
        "opaque": true,
        "metrics": [
          "opnsense_trafficshaper_pipe_active_flows",
          "opnsense_trafficshaper_pipe_packets",
          "opnsense_trafficshaper_pipe_bytes",
          "opnsense_trafficshaper_pipe_drop_packets",
          "opnsense_trafficshaper_pipe_drop_bytes",
          "opnsense_trafficshaper_pipe_configured_bandwidth_bps",
          "opnsense_trafficshaper_pipe_configured_burst_bytes",
          "opnsense_trafficshaper_pipe_configured_delay_milliseconds",
          "opnsense_trafficshaper_pipe_configured_queue_size",
          "opnsense_trafficshaper_pipe_configured_weight",
          "opnsense_trafficshaper_queue_active_flows",
          "opnsense_trafficshaper_queue_packets",
          "opnsense_trafficshaper_queue_bytes",
          "opnsense_trafficshaper_queue_drop_packets",
          "opnsense_trafficshaper_queue_drop_bytes",
          "opnsense_trafficshaper_queue_configured_queue_size",
          "opnsense_trafficshaper_queue_configured_weight"
        ],
        "exercise": "Configure the shaper and then put traffic through it: one pipe with a bandwidth limit, one queue on that pipe, and one shaper RULE attaching them to an interface (Firewall > Shaper > Pipes / Queues / Rules), apply, then generate traffic across that interface from the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). api/trafficshaper/service/statistics answers {status:ok,items:[]} when the shaper is unconfigured, which is the current state of both testbeds. The pipe/queue id label on every one of these series, and the fallback join key (via its dot-prefix) when a queue row carries no pipe field. A pipe item with an empty id is skipped entirely.",
        "blocker": "CORRECTED 2026-08-01 (#621): the previous text here claimed the shaper is not configured on either testbed and that items[] is empty. Both were FALSE and cost a hunt for a configuration that was already there - pipe 10000 @ 200 Mbit/s and rule 'shape TESTLAN' on opt1, which had passed 1.13M packets. items[] has never been empty. What was empty is items[].flows[], because dnctl reports LIVE flows and this pipe has 0 ms delay, so a packet is dequeued almost on arrival and the row lapses between packets; traffgen.sh runs once a MINUTE from cron, so the canary usually polled between bursts. Held open now by shaper-keepalive@.service on the traffgen LXC (a continuous 100 pps / 500-byte ping to each firewall's TESTLAN address - one host covers both, since TESTLAN is a shared segment), measured 12/12 populated where a 2-second interval was 3/5. flex type (KindAny), so even a populated run can only ever prove the key exists; the value evidence is trafficshaper_test.go's fixtures, which are derived from ServiceController.php::statisticsAction and scripts/shaper/lib/__init__.py rather than from a capture."
      },
      {
        "path": "items[].description",
        "opaque": true,
        "metrics": [
          "opnsense_trafficshaper_pipe_active_flows",
          "opnsense_trafficshaper_pipe_packets",
          "opnsense_trafficshaper_pipe_bytes",
          "opnsense_trafficshaper_pipe_drop_packets",
          "opnsense_trafficshaper_pipe_drop_bytes",
          "opnsense_trafficshaper_pipe_configured_bandwidth_bps",
          "opnsense_trafficshaper_pipe_configured_burst_bytes",
          "opnsense_trafficshaper_pipe_configured_delay_milliseconds",
          "opnsense_trafficshaper_pipe_configured_queue_size",
          "opnsense_trafficshaper_pipe_configured_weight",
          "opnsense_trafficshaper_queue_active_flows",
          "opnsense_trafficshaper_queue_packets",
          "opnsense_trafficshaper_queue_bytes",
          "opnsense_trafficshaper_queue_drop_packets",
          "opnsense_trafficshaper_queue_drop_bytes",
          "opnsense_trafficshaper_queue_configured_queue_size",
          "opnsense_trafficshaper_queue_configured_weight"
        ],
        "exercise": "Configure the shaper and then put traffic through it: one pipe with a bandwidth limit, one queue on that pipe, and one shaper RULE attaching them to an interface (Firewall > Shaper > Pipes / Queues / Rules), apply, then generate traffic across that interface from the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). api/trafficshaper/service/statistics answers {status:ok,items:[]} when the shaper is unconfigured, which is the current state of both testbeds. The description label on every pipe and queue series.",
        "blocker": "CORRECTED 2026-08-01 (#621): the previous text here claimed the shaper is not configured on either testbed and that items[] is empty. Both were FALSE and cost a hunt for a configuration that was already there - pipe 10000 @ 200 Mbit/s and rule 'shape TESTLAN' on opt1, which had passed 1.13M packets. items[] has never been empty. What was empty is items[].flows[], because dnctl reports LIVE flows and this pipe has 0 ms delay, so a packet is dequeued almost on arrival and the row lapses between packets; traffgen.sh runs once a MINUTE from cron, so the canary usually polled between bursts. Held open now by shaper-keepalive@.service on the traffgen LXC (a continuous 100 pps / 500-byte ping to each firewall's TESTLAN address - one host covers both, since TESTLAN is a shared segment), measured 12/12 populated where a 2-second interval was 3/5. flex type (KindAny), so even a populated run can only ever prove the key exists; the value evidence is trafficshaper_test.go's fixtures, which are derived from ServiceController.php::statisticsAction and scripts/shaper/lib/__init__.py rather than from a capture."
      },
      {
        "path": "items[].pipe",
        "opaque": true,
        "metrics": [
          "opnsense_trafficshaper_queue_active_flows",
          "opnsense_trafficshaper_queue_packets",
          "opnsense_trafficshaper_queue_bytes",
          "opnsense_trafficshaper_queue_drop_packets",
          "opnsense_trafficshaper_queue_drop_bytes",
          "opnsense_trafficshaper_queue_configured_queue_size",
          "opnsense_trafficshaper_queue_configured_weight",
          "opnsense_trafficshaper_pipe_active_flows",
          "opnsense_trafficshaper_pipe_packets",
          "opnsense_trafficshaper_pipe_bytes",
          "opnsense_trafficshaper_pipe_drop_packets",
          "opnsense_trafficshaper_pipe_drop_bytes",
          "opnsense_trafficshaper_pipe_configured_bandwidth_bps",
          "opnsense_trafficshaper_pipe_configured_burst_bytes",
          "opnsense_trafficshaper_pipe_configured_delay_milliseconds",
          "opnsense_trafficshaper_pipe_configured_queue_size",
          "opnsense_trafficshaper_pipe_configured_weight"
        ],
        "exercise": "Configure the shaper and then put traffic through it: one pipe with a bandwidth limit, one queue on that pipe, and one shaper RULE attaching them to an interface (Firewall > Shaper > Pipes / Queues / Rules), apply, then generate traffic across that interface from the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). api/trafficshaper/service/statistics answers {status:ok,items:[]} when the shaper is unconfigured, which is the current state of both testbeds. The pipe label on the queue series, and the link that attributes a template queue's flow stats and queue_size/weight to its owning pipe - so it feeds the pipe series too. When absent the code falls back to the id prefix before the first dot, which means a rename degrades to a plausible-looking wrong attribution rather than an error.",
        "blocker": "CORRECTED 2026-08-01 (#621): the previous text here claimed the shaper is not configured on either testbed and that items[] is empty. Both were FALSE and cost a hunt for a configuration that was already there - pipe 10000 @ 200 Mbit/s and rule 'shape TESTLAN' on opt1, which had passed 1.13M packets. items[] has never been empty. What was empty is items[].flows[], because dnctl reports LIVE flows and this pipe has 0 ms delay, so a packet is dequeued almost on arrival and the row lapses between packets; traffgen.sh runs once a MINUTE from cron, so the canary usually polled between bursts. Held open now by shaper-keepalive@.service on the traffgen LXC (a continuous 100 pps / 500-byte ping to each firewall's TESTLAN address - one host covers both, since TESTLAN is a shared segment), measured 12/12 populated where a 2-second interval was 3/5. flex type (KindAny), so even a populated run can only ever prove the key exists; the value evidence is trafficshaper_test.go's fixtures, which are derived from ServiceController.php::statisticsAction and scripts/shaper/lib/__init__.py rather than from a capture."
      },
      {
        "path": "items[].template",
        "opaque": true,
        "metrics": [
          "opnsense_trafficshaper_pipe_active_flows",
          "opnsense_trafficshaper_pipe_packets",
          "opnsense_trafficshaper_pipe_bytes",
          "opnsense_trafficshaper_pipe_drop_packets",
          "opnsense_trafficshaper_pipe_drop_bytes",
          "opnsense_trafficshaper_pipe_configured_bandwidth_bps",
          "opnsense_trafficshaper_pipe_configured_burst_bytes",
          "opnsense_trafficshaper_pipe_configured_delay_milliseconds",
          "opnsense_trafficshaper_pipe_configured_queue_size",
          "opnsense_trafficshaper_pipe_configured_weight",
          "opnsense_trafficshaper_rule_packets_total",
          "opnsense_trafficshaper_rule_bytes_total",
          "opnsense_trafficshaper_rule_last_match_timestamp_seconds"
        ],
        "exercise": "Configure the shaper and then put traffic through it: one pipe with a bandwidth limit, one queue on that pipe, and one shaper RULE attaching them to an interface (Firewall > Shaper > Pipes / Queues / Rules), apply, then generate traffic across that interface from the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). api/trafficshaper/service/statistics answers {status:ok,items:[]} when the shaper is unconfigured, which is the current state of both testbeds. statisticsAction moves every pipe's flows onto a synthetic template:true queue item, so configuring a single pipe already produces one. It is a discriminator twice over: template rows are EXCLUDED from the queue metrics (or their flows would be double-counted) and their flows are attributed to the pipe instead, and it also sets the target_type label on every rule series.",
        "blocker": "CORRECTED 2026-08-01 (#621): the previous text here claimed the shaper is not configured on either testbed and that items[] is empty. Both were FALSE and cost a hunt for a configuration that was already there - pipe 10000 @ 200 Mbit/s and rule 'shape TESTLAN' on opt1, which had passed 1.13M packets. items[] has never been empty. What was empty is items[].flows[], because dnctl reports LIVE flows and this pipe has 0 ms delay, so a packet is dequeued almost on arrival and the row lapses between packets; traffgen.sh runs once a MINUTE from cron, so the canary usually polled between bursts. Held open now by shaper-keepalive@.service on the traffgen LXC (a continuous 100 pps / 500-byte ping to each firewall's TESTLAN address - one host covers both, since TESTLAN is a shared segment), measured 12/12 populated where a 2-second interval was 3/5. flex type (KindAny), so even a populated run can only ever prove the key exists; the value evidence is trafficshaper_test.go's fixtures, which are derived from ServiceController.php::statisticsAction and scripts/shaper/lib/__init__.py rather than from a capture."
      },
      {
        "path": "items[].flows[].pkt",
        "metrics": [
          "opnsense_trafficshaper_pipe_packets",
          "opnsense_trafficshaper_queue_packets"
        ],
        "exercise": "Configure the shaper and then put traffic through it: one pipe with a bandwidth limit, one queue on that pipe, and one shaper RULE attaching them to an interface (Firewall > Shaper > Pipes / Queues / Rules), apply, then generate traffic across that interface from the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). api/trafficshaper/service/statistics answers {status:ok,items:[]} when the shaper is unconfigured, which is the current state of both testbeds. Summed across the item's flow rows. TIMING MATTERS: dnctl reports LIVE flows, so an idle shaper serves its pipe and queue items with an empty flows array - active_flows reads a correct 0 and the four flow counters stay unexercised. Traffic has to be moving at the moment the canary polls.",
        "blocker": "CORRECTED 2026-08-01 (#621): the previous text here claimed the shaper is not configured on either testbed and that items[] is empty. Both were FALSE and cost a hunt for a configuration that was already there - pipe 10000 @ 200 Mbit/s and rule 'shape TESTLAN' on opt1, which had passed 1.13M packets. items[] has never been empty. What was empty is items[].flows[], because dnctl reports LIVE flows and this pipe has 0 ms delay, so a packet is dequeued almost on arrival and the row lapses between packets; traffgen.sh runs once a MINUTE from cron, so the canary usually polled between bursts. Held open now by shaper-keepalive@.service on the traffgen LXC (a continuous 100 pps / 500-byte ping to each firewall's TESTLAN address - one host covers both, since TESTLAN is a shared segment), measured 12/12 populated where a 2-second interval was 3/5."
      },
      {
        "path": "items[].flows[].bytes",
        "metrics": [
          "opnsense_trafficshaper_pipe_bytes",
          "opnsense_trafficshaper_queue_bytes"
        ],
        "exercise": "Configure the shaper and then put traffic through it: one pipe with a bandwidth limit, one queue on that pipe, and one shaper RULE attaching them to an interface (Firewall > Shaper > Pipes / Queues / Rules), apply, then generate traffic across that interface from the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). api/trafficshaper/service/statistics answers {status:ok,items:[]} when the shaper is unconfigured, which is the current state of both testbeds. Summed across the item's flow rows. TIMING MATTERS: dnctl reports LIVE flows, so an idle shaper serves its pipe and queue items with an empty flows array - active_flows reads a correct 0 and the four flow counters stay unexercised. Traffic has to be moving at the moment the canary polls.",
        "blocker": "CORRECTED 2026-08-01 (#621): the previous text here claimed the shaper is not configured on either testbed and that items[] is empty. Both were FALSE and cost a hunt for a configuration that was already there - pipe 10000 @ 200 Mbit/s and rule 'shape TESTLAN' on opt1, which had passed 1.13M packets. items[] has never been empty. What was empty is items[].flows[], because dnctl reports LIVE flows and this pipe has 0 ms delay, so a packet is dequeued almost on arrival and the row lapses between packets; traffgen.sh runs once a MINUTE from cron, so the canary usually polled between bursts. Held open now by shaper-keepalive@.service on the traffgen LXC (a continuous 100 pps / 500-byte ping to each firewall's TESTLAN address - one host covers both, since TESTLAN is a shared segment), measured 12/12 populated where a 2-second interval was 3/5."
      },
      {
        "path": "items[].flows[].drop_pkt",
        "metrics": [
          "opnsense_trafficshaper_pipe_drop_packets",
          "opnsense_trafficshaper_queue_drop_packets"
        ],
        "exercise": "Configure the shaper and then put traffic through it: one pipe with a bandwidth limit, one queue on that pipe, and one shaper RULE attaching them to an interface (Firewall > Shaper > Pipes / Queues / Rules), apply, then generate traffic across that interface from the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). api/trafficshaper/service/statistics answers {status:ok,items:[]} when the shaper is unconfigured, which is the current state of both testbeds. Summed across the item's flow rows, and only non-zero once the pipe is actually saturated - so the bandwidth limit needs to be low enough that the generated traffic exceeds it. TIMING MATTERS: dnctl reports LIVE flows, so an idle shaper serves its pipe and queue items with an empty flows array - active_flows reads a correct 0 and the four flow counters stay unexercised. Traffic has to be moving at the moment the canary polls.",
        "blocker": "CORRECTED 2026-08-01 (#621): the previous text here claimed the shaper is not configured on either testbed and that items[] is empty. Both were FALSE and cost a hunt for a configuration that was already there - pipe 10000 @ 200 Mbit/s and rule 'shape TESTLAN' on opt1, which had passed 1.13M packets. items[] has never been empty. What was empty is items[].flows[], because dnctl reports LIVE flows and this pipe has 0 ms delay, so a packet is dequeued almost on arrival and the row lapses between packets; traffgen.sh runs once a MINUTE from cron, so the canary usually polled between bursts. Held open now by shaper-keepalive@.service on the traffgen LXC (a continuous 100 pps / 500-byte ping to each firewall's TESTLAN address - one host covers both, since TESTLAN is a shared segment), measured 12/12 populated where a 2-second interval was 3/5."
      },
      {
        "path": "items[].flows[].drop_bytes",
        "metrics": [
          "opnsense_trafficshaper_pipe_drop_bytes",
          "opnsense_trafficshaper_queue_drop_bytes"
        ],
        "exercise": "Configure the shaper and then put traffic through it: one pipe with a bandwidth limit, one queue on that pipe, and one shaper RULE attaching them to an interface (Firewall > Shaper > Pipes / Queues / Rules), apply, then generate traffic across that interface from the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). api/trafficshaper/service/statistics answers {status:ok,items:[]} when the shaper is unconfigured, which is the current state of both testbeds. Same as items[].flows[].drop_pkt.",
        "blocker": "CORRECTED 2026-08-01 (#621): the previous text here claimed the shaper is not configured on either testbed and that items[] is empty. Both were FALSE and cost a hunt for a configuration that was already there - pipe 10000 @ 200 Mbit/s and rule 'shape TESTLAN' on opt1, which had passed 1.13M packets. items[] has never been empty. What was empty is items[].flows[], because dnctl reports LIVE flows and this pipe has 0 ms delay, so a packet is dequeued almost on arrival and the row lapses between packets; traffgen.sh runs once a MINUTE from cron, so the canary usually polled between bursts. Held open now by shaper-keepalive@.service on the traffgen LXC (a continuous 100 pps / 500-byte ping to each firewall's TESTLAN address - one host covers both, since TESTLAN is a shared segment), measured 12/12 populated where a 2-second interval was 3/5."
      },
      {
        "path": "items[].rules[].rule",
        "opaque": true,
        "metrics": [
          "opnsense_trafficshaper_rule_packets_total",
          "opnsense_trafficshaper_rule_bytes_total",
          "opnsense_trafficshaper_rule_last_match_timestamp_seconds"
        ],
        "exercise": "Configure the shaper and then put traffic through it: one pipe with a bandwidth limit, one queue on that pipe, and one shaper RULE attaching them to an interface (Firewall > Shaper > Pipes / Queues / Rules), apply, then generate traffic across that interface from the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). api/trafficshaper/service/statistics answers {status:ok,items:[]} when the shaper is unconfigured, which is the current state of both testbeds. Needs a shaper RULE, not just a pipe and a queue: an unattached pipe serves an empty rules array. This is the rule label on all three rule series.",
        "blocker": "CORRECTED 2026-08-01 (#621): the previous text here claimed the shaper is not configured on either testbed and that items[] is empty. Both were FALSE and cost a hunt for a configuration that was already there - pipe 10000 @ 200 Mbit/s and rule 'shape TESTLAN' on opt1, which had passed 1.13M packets. items[] has never been empty. What was empty is items[].flows[], because dnctl reports LIVE flows and this pipe has 0 ms delay, so a packet is dequeued almost on arrival and the row lapses between packets; traffgen.sh runs once a MINUTE from cron, so the canary usually polled between bursts. Held open now by shaper-keepalive@.service on the traffgen LXC (a continuous 100 pps / 500-byte ping to each firewall's TESTLAN address - one host covers both, since TESTLAN is a shared segment), measured 12/12 populated where a 2-second interval was 3/5. flex type (KindAny), so even a populated run can only ever prove the key exists; the value evidence is trafficshaper_test.go's fixtures, which are derived from ServiceController.php::statisticsAction and scripts/shaper/lib/__init__.py rather than from a capture."
      },
      {
        "path": "items[].rules[].attached_to",
        "opaque": true,
        "metrics": [
          "opnsense_trafficshaper_rule_packets_total",
          "opnsense_trafficshaper_rule_bytes_total",
          "opnsense_trafficshaper_rule_last_match_timestamp_seconds"
        ],
        "exercise": "Configure the shaper and then put traffic through it: one pipe with a bandwidth limit, one queue on that pipe, and one shaper RULE attaching them to an interface (Firewall > Shaper > Pipes / Queues / Rules), apply, then generate traffic across that interface from the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). api/trafficshaper/service/statistics answers {status:ok,items:[]} when the shaper is unconfigured, which is the current state of both testbeds. The attached_to label on all three rule series; needs a shaper rule as above.",
        "blocker": "CORRECTED 2026-08-01 (#621): the previous text here claimed the shaper is not configured on either testbed and that items[] is empty. Both were FALSE and cost a hunt for a configuration that was already there - pipe 10000 @ 200 Mbit/s and rule 'shape TESTLAN' on opt1, which had passed 1.13M packets. items[] has never been empty. What was empty is items[].flows[], because dnctl reports LIVE flows and this pipe has 0 ms delay, so a packet is dequeued almost on arrival and the row lapses between packets; traffgen.sh runs once a MINUTE from cron, so the canary usually polled between bursts. Held open now by shaper-keepalive@.service on the traffgen LXC (a continuous 100 pps / 500-byte ping to each firewall's TESTLAN address - one host covers both, since TESTLAN is a shared segment), measured 12/12 populated where a 2-second interval was 3/5. flex type (KindAny), so even a populated run can only ever prove the key exists; the value evidence is trafficshaper_test.go's fixtures, which are derived from ServiceController.php::statisticsAction and scripts/shaper/lib/__init__.py rather than from a capture."
      },
      {
        "path": "items[].rules[].description",
        "opaque": true,
        "metrics": [
          "opnsense_trafficshaper_rule_packets_total",
          "opnsense_trafficshaper_rule_bytes_total",
          "opnsense_trafficshaper_rule_last_match_timestamp_seconds"
        ],
        "exercise": "Configure the shaper and then put traffic through it: one pipe with a bandwidth limit, one queue on that pipe, and one shaper RULE attaching them to an interface (Firewall > Shaper > Pipes / Queues / Rules), apply, then generate traffic across that interface from the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). api/trafficshaper/service/statistics answers {status:ok,items:[]} when the shaper is unconfigured, which is the current state of both testbeds. The description label on all three rule series; needs a shaper rule as above.",
        "blocker": "CORRECTED 2026-08-01 (#621): the previous text here claimed the shaper is not configured on either testbed and that items[] is empty. Both were FALSE and cost a hunt for a configuration that was already there - pipe 10000 @ 200 Mbit/s and rule 'shape TESTLAN' on opt1, which had passed 1.13M packets. items[] has never been empty. What was empty is items[].flows[], because dnctl reports LIVE flows and this pipe has 0 ms delay, so a packet is dequeued almost on arrival and the row lapses between packets; traffgen.sh runs once a MINUTE from cron, so the canary usually polled between bursts. Held open now by shaper-keepalive@.service on the traffgen LXC (a continuous 100 pps / 500-byte ping to each firewall's TESTLAN address - one host covers both, since TESTLAN is a shared segment), measured 12/12 populated where a 2-second interval was 3/5. flex type (KindAny), so even a populated run can only ever prove the key exists; the value evidence is trafficshaper_test.go's fixtures, which are derived from ServiceController.php::statisticsAction and scripts/shaper/lib/__init__.py rather than from a capture."
      },
      {
        "path": "items[].rules[].pkts",
        "metrics": [
          "opnsense_trafficshaper_rule_packets_total"
        ],
        "exercise": "Configure the shaper and then put traffic through it: one pipe with a bandwidth limit, one queue on that pipe, and one shaper RULE attaching them to an interface (Firewall > Shaper > Pipes / Queues / Rules), apply, then generate traffic across that interface from the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). api/trafficshaper/service/statistics answers {status:ok,items:[]} when the shaper is unconfigured, which is the current state of both testbeds. The ipfw rule's own packet counter - unlike the flow counters this one is cumulative and survives an idle moment, so it only needs the rule to have matched traffic at some point, not to be matching right now.",
        "blocker": "CORRECTED 2026-08-01 (#621): the previous text here claimed the shaper is not configured on either testbed and that items[] is empty. Both were FALSE and cost a hunt for a configuration that was already there - pipe 10000 @ 200 Mbit/s and rule 'shape TESTLAN' on opt1, which had passed 1.13M packets. items[] has never been empty. What was empty is items[].flows[], because dnctl reports LIVE flows and this pipe has 0 ms delay, so a packet is dequeued almost on arrival and the row lapses between packets; traffgen.sh runs once a MINUTE from cron, so the canary usually polled between bursts. Held open now by shaper-keepalive@.service on the traffgen LXC (a continuous 100 pps / 500-byte ping to each firewall's TESTLAN address - one host covers both, since TESTLAN is a shared segment), measured 12/12 populated where a 2-second interval was 3/5."
      },
      {
        "path": "items[].rules[].bytes",
        "metrics": [
          "opnsense_trafficshaper_rule_bytes_total"
        ],
        "exercise": "Configure the shaper and then put traffic through it: one pipe with a bandwidth limit, one queue on that pipe, and one shaper RULE attaching them to an interface (Firewall > Shaper > Pipes / Queues / Rules), apply, then generate traffic across that interface from the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). api/trafficshaper/service/statistics answers {status:ok,items:[]} when the shaper is unconfigured, which is the current state of both testbeds. Same as items[].rules[].pkts.",
        "blocker": "CORRECTED 2026-08-01 (#621): the previous text here claimed the shaper is not configured on either testbed and that items[] is empty. Both were FALSE and cost a hunt for a configuration that was already there - pipe 10000 @ 200 Mbit/s and rule 'shape TESTLAN' on opt1, which had passed 1.13M packets. items[] has never been empty. What was empty is items[].flows[], because dnctl reports LIVE flows and this pipe has 0 ms delay, so a packet is dequeued almost on arrival and the row lapses between packets; traffgen.sh runs once a MINUTE from cron, so the canary usually polled between bursts. Held open now by shaper-keepalive@.service on the traffgen LXC (a continuous 100 pps / 500-byte ping to each firewall's TESTLAN address - one host covers both, since TESTLAN is a shared segment), measured 12/12 populated where a 2-second interval was 3/5."
      },
      {
        "path": "items[].rules[].accessed_epoch",
        "metrics": [
          "opnsense_trafficshaper_rule_last_match_timestamp_seconds"
        ],
        "exercise": "Configure the shaper and then put traffic through it: one pipe with a bandwidth limit, one queue on that pipe, and one shaper RULE attaching them to an interface (Firewall > Shaper > Pipes / Queues / Rules), apply, then generate traffic across that interface from the traffgen LXC on the isolated bridge is the only traffic source available (the Proxmox host is booted with ipv6.disable=1, so that LXC can never hold an IPv6 lease or terminate a v6 tunnel - only a VM can; see the keaLeases6 note). api/trafficshaper/service/statistics answers {status:ok,items:[]} when the shaper is unconfigured, which is the current state of both testbeds. 0 until the rule first matches traffic - a sentinel, not the Unix epoch (#224) - and the metric is presence-gated on a value above 0, so the series does not exist at all until real traffic has crossed the rule. Generating traffic is therefore mandatory for this path, not merely for a non-zero value.",
        "blocker": "CORRECTED 2026-08-01 (#621): the previous text here claimed the shaper is not configured on either testbed and that items[] is empty. Both were FALSE and cost a hunt for a configuration that was already there - pipe 10000 @ 200 Mbit/s and rule 'shape TESTLAN' on opt1, which had passed 1.13M packets. items[] has never been empty. What was empty is items[].flows[], because dnctl reports LIVE flows and this pipe has 0 ms delay, so a packet is dequeued almost on arrival and the row lapses between packets; traffgen.sh runs once a MINUTE from cron, so the canary usually polled between bursts. Held open now by shaper-keepalive@.service on the traffgen LXC (a continuous 100 pps / 500-byte ping to each firewall's TESTLAN address - one host covers both, since TESTLAN is a shared segment), measured 12/12 populated where a 2-second interval was 3/5."
      }
    ],
    "note": "AUDITED 2026-07-31 (#599). 15 of the 19 unclassified paths on this endpoint back a metric; the other four - items[], items[].flows, items[].flows[], items[].rules, items[].rules[] - are array or element containers carrying no value and are left informational, resolving with the leaves above. One nuance recorded so it is not mistaken for a gap: the CARDINALITY of items[].flows[] is itself the value of pipe_active_flows/queue_active_flows (the code counts flow rows), but an empty flows array is a legitimate zero rather than a blind spot, so it still does not warrant an entry. bw/delay/burst/queue_size/weight are absent from this list because they were only modelled in #584 and are covered by their own paths, which this run reported under the same empty items[] - if they appear in a future unclassified bucket they belong here too, on the same reasoning as their siblings."
  }
}
